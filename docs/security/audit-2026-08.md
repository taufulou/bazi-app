# Security Audit — Pre-Launch (2026-08)

Living document for the launch-readiness security pass. Plan: `~/.claude/plans/launch-scalability-security-plan.md`.
Branch: `claude/bazi-scalability-security-e4ff78`. Threat model priority: **denial-of-wallet** (AI generation is the expensive asset), then user PII (birth data), then IP, then availability.

**Status**: Phase 1A in progress. A1 ✅ · A5 ✅ · A8 ✅ · A2/A3/A4/A6/A7 pending · Phase 1B/1C/1D pending.

---

## A1 — Generation-cost map

Every route that can reach a paid AI provider. "Payment gate" = a server-side credit deduction or subscription check that happens **before** generation.

### Bazi readings & comparisons (`apps/api/src/bazi/`)

| Route | Auth | Payment gate | Cache-first | Concurrency guard | Verdict |
|---|---|---|---|---|---|
| `POST /api/bazi/readings` | Clerk | ✅ credits deducted in create flow | ✅ reuses existing row | ✅ `reading:create:{userId}` SETNX | ✅ |
| `SSE /api/bazi/readings/:id/stream` | Clerk | ⚠️ **NONE** — see F2 | ⚠️ emits **full** sections on hit, unconditionally | ✅ **two** layers: per-user cap (max 2 concurrent) + per-reading `stream:reading:{id}` SETNX (330s TTL > 300s AI timeout) | ⚠️ locks strong, **paywall absent** |
| `POST /api/bazi/readings/:id/regenerate` | Clerk | ✅ free **by design**, hard-capped at 3/reading | n/a | ✅ atomic `updateMany` with `isDegraded`, `regenerationExhausted`, `regenerationCount < 3` **all in the WHERE clause** — TOCTOU-safe | ✅ strong |
| `POST /api/bazi/comparisons` | Clerk | ✅ credits at create | ✅ `pairKey` reuse | ✅ `comparison:create:{userId}` | ✅ |
| `POST /api/bazi/comparisons/:id/generate-ai` | Clerk | ✅ idempotent return gated on `aiInterpretation && paidAt !== null` — a refunded or never-paid row does **not** get free content | ✅ | ✅ `ai:generate:comparison:{id}` + poll-for-completion | ✅ strong |
| `SSE /api/bazi/comparisons/:id/stream` | Clerk | ✅ same row semantics | ✅ | ✅ | ✅ |
| `POST /api/bazi/comparisons/:id/recalculate` | Clerk | ✅ 1 credit | — | — | ✅ |
| `POST /api/bazi/calculate` | **@Public** | n/a — engine only, **no AI** | — | 20/min | ✅ (cost is 3ms CPU) |
| `POST /api/bazi/explain-element` | **@Public** | n/a — template lookup, **no AI** | — | 60/min | ⚠️ see B1 (paid Layer C/D exposure) |

**Note on the two SSE routes**: they carry no `@Throttle` of their own, inheriting only the global limit. Acceptable because generation is gated on row state + two lock layers, but flagged for M1 (the global limiter is currently one shared bucket — see plan).

### Fortune (`apps/api/src/fortune/`)

| Route | Auth | Payment gate | Cache-first |
|---|---|---|---|
| `GET /api/fortune/{daily,monthly,yearly}` + `/stream` (6 routes) | Clerk | ✅ `enforce{,Monthly,Yearly}SubscriptionGate` (tier + date-window) | ✅ Redis → DB snapshot → engine → AI |

Free tier is limited to the current period, so a free user's narration count is bounded by profile count — which is why **A4a (birth-profile cap)** matters. All 6 throttled at 10/min.

### Chat (`apps/api/src/chat/`)

| Route | Auth | Payment gate |
|---|---|---|
| `POST /api/chat/sessions` | Clerk | ✅ 5/hour throttle + session caps |
| `POST /api/chat/sessions/:id/messages{,-sync}` | Clerk | ✅ `deductForMessage` — subscription quota or per-extension credits |
| `POST /api/chat/sessions/:id/extend` | Clerk | ✅ 1 credit / 10 messages, Redis SETNX de-dupes concurrent buys |

### Verdict

**No unauthenticated route can trigger AI generation** — verified: every AI path sits behind Clerk, and the *generation* paths carry a server-side charge, cache-first checks, and distributed locks. Generation-cost control is sound.

⚠️ **Corrected after line audit (2026-08-13):** an earlier revision of this section graded the reading-stream row "✅ strong". That was wrong, and wrong in the dangerous direction. Grading the *generation* controls green, I did not separately check the *content-delivery* dimension — and `bazi.service.ts:198-199` says outright that `/readings/:id/stream` has **no payment gate**. **Charging for generation and gating access to content are two different controls; this codebase has the first and not the second.** See F2/F3 below. An audit doc that overstates safety is worse than no doc, because it becomes the artifact the next reviewer trusts instead of the code.

---

## A5 — E2E auth-bypass audit

`grep -rn "__e2e_auth" apps/api/src` → **0 hits**. ✅ The Playwright cookie bypass is confined to web UI components and never reaches an API authorization decision.

---

## A8 — Rewarded-ad free-value vectors ✅ FIXED

Two vectors, same root cause: **V1 ads do no ad-completion verification** (`ads.service.ts` docblock: *"Trust client callback (mock ads). No server-side ad completion verification."*).

| # | Vector | Severity | Detail |
|---|---|---|---|
| 1 | `POST /api/payments/readings/:id/unlock-section` with `method: 'ad_reward'` | **MEDIUM** (downgraded from HIGH — see note) | Created a `SectionUnlock` with `creditsUsed: 0` and verified nothing — no `AdRewardLog` lookup, no SSV. (`section-unlock.service.ts:215-229`) **Severity note:** the original HIGH rating assumed unlock rows control content access. They do not — `SectionUnlock` is read by nothing on the delivery path (F3), and an owner already receives every section in full (F2). So this closes a *credit-avoidance / row-creation* path, not a content-access path. The mirror-image problem is that the surviving `credit` method debits real credits for a row that grants nothing — a refund liability. |
| 2 | `POST /api/ads/claim` with `rewardType: 'CREDIT'` | **HIGH** — *found during A1, not in the original plan* | **Mints real credits** (`CREDITS_PER_AD_VIEW`) to any authenticated caller with no proof an ad was shown. Bounded per account at `MAX_DAILY_AD_VIEWS = 5`/day, but Clerk signup is free ⇒ unbounded across accounts. Credits buy readings ⇒ real Anthropic spend **and** direct revenue loss (credits have a `CreditPackage.priceUsd`). |

**Blast radius before fix**: 5 credits/day/account × N throwaway accounts. At ~1–3 credits per reading and ~$0.20/reading, 100 accounts ≈ 500 credits/day ≈ $50–100/day of Anthropic spend plus the revenue never collected.

**Fix**: single kill switch `ADS_REWARDS_ENABLED` (Joi-validated `'0'|'1'`, **default `'0'`**) gating both. Verified safe: **no client CALLS `/api/ads/*` or sends `ad_reward`** in `apps/web` or `apps/mobile`, so gating is behaviour-preserving for real traffic. (Precise wording matters: `apps/web/.../InsufficientCreditsModal.tsx:113-121` does render a 「看廣告獲得 1 點」 button — but it is `disabled` with no `onClick` and constructs no URL. `apps/mobile` has no ad surface at all. Whoever wires that button up must know this flag exists.)

Design notes:
- The DTO **still accepts** `method: 'ad_reward'`. A static `@IsIn` decorator cannot be env-toggled, so rejecting there would make the flag a lie and re-enabling a code change. Enforcement lives in the service.
- In `AdsService.claimReward` the switch fires **before** the Redis daily counter, so a blocked call costs no quota and leaks no argument oracle.
- ⚠️ **Corrected** (this line previously claimed ownership resolves *before* the switch, contradicting the F3 section below and the code). `sectionUnlockEnabled()` is the **first statement** of `unlockSection` (`section-unlock.service.ts:147`), ~44 lines ahead of the ownership check at `:191` — F3 is right, this was wrong. With `SECTION_UNLOCK_ENABLED=0` (the production default) the whole method refuses before any DB read, so it leaks no existence oracle at all. The ordering claim only ever mattered in the flag-on configuration, which is not deployed. (A pre-existing 404-vs-403 split at `:144-151` distinguishes "no such reading" from "someone else's" — negligible with UUID ids, noted rather than fixed.)
- Both throw `{ code: 'ADS_REWARDS_DISABLED' }` for client branching.

**Re-enable requires BOTH**: (1) AdMob SSV wired into `claimReward` so a claim proves a real view, and (2) `unlock-section` consuming an unconsumed `AdRewardLog` row scoped to `(userId, readingId, sectionKey)` — the schema already carries those fields. Then set `ADS_REWARDS_ENABLED=1`. **Flipping the env alone re-opens both vectors** — the flag is a kill switch, not a fix.

**Tests**: 17 new (`test/ads-service.kill-switch.spec.ts` 10, `test/section-unlock-service.kill-switch.spec.ts` 7), covering default-off, both flag states, no-value-granted, check ordering, and non-regression of the credit path. The two pre-existing suites now force the flag on and say why (they test the feature, not the switch).

Mutation-tested by the line audit: deleting the ads gate fails 7/10 of its tests; deleting the unlock gate fails 4/7. **One test was found vacuous and fixed** — `'still enforces ownership'` used a negative assertion (`.rejects.not.toMatchObject(...)`, satisfied by *any* rejection) over a mock missing the `sections` wrapper, so the auditor deleted the entire ownership block and all 7 tests still passed. Now asserts `ForbiddenException` positively over a correctly-shaped mock. Ownership itself was never unprotected — `section-unlock-service.spec.ts:283,641` covers it — but the invariant this test claimed to lock was not locked.

⚠️ **Do not count `e2e/ad-rewards.spec.ts` / `e2e/section-unlock.spec.ts` as coverage**: they `fetch('/api/ads/claim')` against the *Next.js* origin, where no such route or rewrite exists, and pass purely off their own `page.route` intercepts. They could not have caught either vector and cannot catch a regression.

---

## FINDINGS FROM THE A8 LINE AUDIT (F1–F4)

Four vectors outside A8's own scope, all pre-existing (none introduced by this branch). Status after the owner's decisions of 2026-08-13:

| | Finding | Status |
|---|---|---|
| F1 | `ensureUser` re-mints the signup bonus after deletion | ✅ **RESOLVED** |
| F2 | Reading paywall is dead code | ✅ **RESOLVED** — owner chose Option C |
| F3 | `SectionUnlock` grants nothing | ✅ **RESOLVED** — disabled |
| F4 | Cross-user cache reuse is free | ✅ **NOT A DEFECT** — intended pricing |

### F1 — ✅ RESOLVED — signup bonus was re-mintable after account deletion `[medium]`
`users.service.ts:450-464` auto-creates a user row with `credits: 3` whenever no row matches the Clerk id (a "webhook not configured" fallback), and it is reachable from most authenticated routes (`:188,196,227,267`). `deleteAccount` does **not** delete the row — it renames `clerkUserId` to `deleted_<id>_<ts>` and zeroes credits (`:113-122`). The Clerk-side delete immediately before it is best-effort: `deleteClerkUser` swallows failures (`:145-147`) and returns early when `CLERK_SECRET_KEY` is unset (`:139-142`). **If the Clerk delete fails, the Clerk identity survives while the DB row is orphaned, and the next authenticated request mints a fresh 3 credits — loopable.** Nothing gates this.
**Fixed** — and the audit undercounted the insert sites: there are **three**, not one. `ensureUser`'s fallback plus **both** `create` branches in the Clerk webhook (`handleUserCreated` and `handleUserUpdated`'s upsert). All three hardcoded `credits: 3`, so all three re-minted.

Rather than disabling the fallback (which would 404 a legitimate new user whenever the Clerk webhook lags — an availability risk at the worst moment, signup), the bonus is now **resolved once per identity** via `src/common/signup-bonus.ts::resolveSignupCredits`: it returns 0 when a soft-deleted row exists for that Clerk id (`clerkUserId LIKE 'deleted_<id>_%'`), otherwise the full bonus. The trailing underscore anchors the match so `user_ab` cannot match a deleted `user_abc`. It **fails open** on a lookup error — a database hiccup must not deny a real new user their credits, and the downside is bounded at 3.

No schema change: the `deleted_<id>_<ts>` rename already carries the fact. If a `deletedAt` column is ever added, switch the lookup and drop the prefix matching.

**Not fixed, deliberately**: the Clerk-side delete stays best-effort. `CLERK_SECRET_KEY` is Joi-`required`, so the early-return branch is dev-only, and aborting a user's account deletion because a third-party API is down is the worse failure. Consequence to accept: if the Clerk delete fails, the identity can re-authenticate and get a working (0-credit) account — deletion didn't stick, but no money is minted. Worth revisiting if account-deletion durability becomes a compliance requirement.

Tests: 6 (`test/free-spend-vectors.spec.ts`). Mutation-tested with A4a: neutering both controls fails 6/10.

### F2 — ✅ RESOLVED (owner: Option C) — reading paywall was dead code
`bazi.service.ts:541` fetches with `where: { id, userId: user.id }`, then `:554` gates on `reading.creditsUsed > 0 || reading.userId === user.id`. The second disjunct is **guaranteed true by the WHERE clause**, so `isOwnerReading` is always true, `:557` always returns early, and the preview-stripping branch at `:561-578` is unreachable. The SSE route is worse: `:621-627` emits on `if (reading.aiInterpretation)` with no payment check, and `emitStaticSections` (`:730-751`) sends `full` for every section.

**This exact bug was already found and fixed on the comparison path** — `:1293-1300` documents it verbatim ("could never be false … unreachable dead code") and switched to `paidAt !== null`, and `:813` gates on `aiInterpretation && paidAt !== null` with a comment explaining that keying on the interpretation alone "hands the full report over for free." **The reading twin and `zwds.service.ts:455` never received the fix.**

**Investigating the fix changed its shape.** Refunds fire only on outright generation failure, and the refund also nulls `aiInterpretation` — so the stream defect is not "replays stored text for free". An ungated re-stream of a refunded row runs a **full provider generation**: real Anthropic spend for a reading already refunded, and it **bypasses the 3-per-reading regeneration cap**, which is enforced in `regenerateReading`, not in the stream. That makes the stream the sharper half of F2, and a denial-of-wallet issue rather than only a content one.

**Fix shipped (Option C):**
1. **Entitlement = absence of a refund**, not presence of a charge — `!reading.refundedAt` in `bazi.service.getReading` and `zwds.service.getReading`. Deleting only the tautology was not an option: the residue `creditsUsed > 0` would paywall the 0-credit cache-hit readings F4 makes free.
2. **Stream payment gate** — `_setupStream` throws `READING_REFUNDED` on a refunded row instead of generating.
3. ~~**`regenerateReading` clears `refundedAt` AND zeroes `creditsUsed`**~~ — **REVERTED, see below.**

### ⚠️ F2 item 3 was wrong and is reverted — `regenerateReading` now touches neither field

Both the completeness and the correctness auditor independently flagged this (HIGH, confidence 82 and 90). It was a **self-inflicted regression**, not a pre-existing defect.

The reasoning behind it — "clearing `refundedAt` re-arms the refund guard, so zero the charge too" — was sound in the abstract and **unreachable in practice**. `ai.service.ts` computes ONE exclusive status per attempt: `isDegraded: true` is written only on `'degraded'` (`:1307`), `refundedAt` only on `'failed'` (`:1347`). `regenerateReading`'s WHERE requires `isDegraded: true`, so it can only ever match a row that was **charged and never refunded**. The `refundedAt: null` clear was a no-op on every row it could reach, and `creditsUsed: 0` was pure damage:

- **The user silently eats the charge.** Pay 3 → partial content → click the free retry → retry fails outright → `refundReadingCredit` short-circuits on its own `creditsUsed === 0` guard → no refund, no reading, 3 credits gone. Before this branch, that path refunded. An ordinary path with no adversary.
- **Uncapped free generation.** With `refundedAt` still null the new stream gate never fires, so every `/readings/:id/stream` re-runs a full provider generation — and `createReading` classifies the row as `isRegenerating`, handing back `streamReady` for free indefinitely, so the user cannot even create a fresh charged reading of that type.
- Paid readings render as free in web and mobile history (`creditsUsed === 0`).

**Now:** `regenerateReading` leaves `refundedAt` and `creditsUsed` alone. The double-refund it was reaching for is already prevented by `refundedAt`, which regeneration no longer clears. The refusal message was also corrected — it used to say 「請使用「重新生成」」, a path that always refuses for a refunded row; it now directs the user to create a new reading, which is what actually works.

**Lesson worth keeping:** the fix was defended by a test asserting exactly the behaviour it implemented, over a hand-built row (`creditsUsed: 0, refundedAt: null`) that the production state machine cannot produce. Reachability has to be established from the state machine, not assumed from the field names.

**Truthiness, not `=== null`.** Prisma returns `null` for an unset `DateTime?`, but a partial `select` or an incomplete mock yields `undefined`, and `undefined === null` is false — which silently paywalls **paying** customers. The strict version was written first and the zwds suite caught it: two "owner gets full content" tests flipped to preview. Locked by a `refundedAt: undefined` case.

**Tests**: 10 new (`test/reading-paywall.spec.ts`) + the zwds fixtures made realistic + the regenerate arg assertion updated. Mutation-tested: neutering both gates fails exactly the 2 tests that assert them.

### F3 — ✅ RESOLVED (owner decision: **disable**) — `SectionUnlock` rows granted nothing
The only readers of the table are `getUnlockedSections` (`section-unlock.service.ts:85`, metadata only), `getReadingWithSectionAccess` (`:283` — **called from nowhere**, verified by grep across `src/`), and admin stats (`admin.service.ts:947`). Neither `getReading` nor the stream joins it, and `apps/web` never calls `/unlocked-sections`. So the `credit` unlock method debits real credits for a row that changes nothing a user can observe.

**Owner chose (b) withdraw, 2026-08-13.** `unlockSection` now refuses behind `SECTION_UNLOCK_ENABLED` (Joi `'0'|'1'`, **default `'0'`**), checked **first — before any validation**, so a disabled deployment charges nobody and leaks no oracle (not even which section keys or reading types are valid; it never even looks the reading up). The switch **outranks `ADS_REWARDS_ENABLED`**: with the feature off, both `credit` and `ad_reward` refuse. Error code `SECTION_UNLOCK_DISABLED`.

Verified safe to disable:
- **Zero client callers** across `apps/web` + `apps/mobile` (the admin monetization page reads aggregate stats via a separate admin endpoint, which still works).
- **Zero rows in `section_unlocks`** at the time of the change (`SELECT COUNT(*), SUM(credits_used)` → `0|0|0`), so **nobody has ever paid for one — no refund liability**.
- The read path (`getUnlockedSections`) is left working; it simply returns nothing.

Re-enabling requires wiring the table into content delivery first — join `SectionUnlock` in `getReading` and `emitStaticSections`, and fix F2's dead owner-check — **then** set `SECTION_UNLOCK_ENABLED=1`. The flag alone just resumes selling the inert row.

Tests: 5 new (feature-off for both methods, charges-nothing, outranks-ads, fires-before-all-validation). Mutation-tested: neutering the gate fails **5 of 12** tests in the suite.

### F4 — ✅ NOT A DEFECT — cache reuse is **intended pricing** (owner-confirmed)
`bazi.service.ts:374-375` sets `creditsUsed = fromCache ? 0 : cost` and `:393` skips deduction on a cache hit, while `getCachedInterpretation` (`ai.service.ts:7250-7252`) keys on **`birthDataHash + readingType` with no `userId`**. So a second user with identical birth data (same date/time/city/gender) receives the full interpretation for **0 credits**. Birth data is not secret — a public figure's, or a family member's, is guessable/shareable.

**Owner confirmed 2026-08-13: intended.** *"same birth data won't charge twice, will reuse same data."* Recording the exact semantics so this isn't re-litigated or accidentally broken:

- The cache is keyed on **`birthDataHash + readingType` with no `userId`**, so reuse is **cross-user**, not just same-user. Two different accounts submitting identical birth data (date, time, city, gender) result in **one charge total** — the second is served free from the first's interpretation. This is accepted.
- ⚠️ **Binding constraint on the F2 fix**: a 0-credit cache-hit row is a *legitimate* paid-equivalent state. When A2 removes the tautological owner-check, it must **not** start treating `creditsUsed === 0` as unpaid — that would paywall exactly the readings this pricing decision says are free. Gate on an explicit paid/refunded marker (the comparison path's `paidAt !== null` pattern), never on `creditsUsed > 0`.
- Residual accepted risk, noted not actioned: birth data is guessable/shareable, so a determined user can obtain a free reading by reusing someone else's birth details. Bounded — it yields the *same* chart, not a new one — and it saves the Anthropic spend it would otherwise cost, which is the point of the cache.

---

## A4 — free-spend vectors ✅

### A4a — birth-profile cap ✅
`BIRTH_PROFILE_MAX_PER_USER` (Joi int ≥1, **default 10**) enforced in `createBirthProfile`, rejecting with `BIRTH_PROFILE_LIMIT_REACHED`.

⚠️ **Correction:** an earlier version of this line said a junk/negative/zero env value "falls back to 10". It does not — `Joi.number().integer().min(1)` (`app.module.ts:266`) **rejects it at boot and the app fails to start**. The `Number.isFinite(parsed) && parsed > 0 ? parsed : 10` guard in `users.service.ts` is unreachable defence-in-depth behind Joi, and the test that appears to lock it injects a mock `ConfigService`, bypassing validation entirely. Both behaviours are safe; only the documented one was wrong. The half that would matter if Joi were ever relaxed is `isFinite` — `10 >= NaN` is false, so a NaN cap would disable the control rather than fail closed.

**Deliberately not race-proof.** Two concurrent creates can both observe 9 and yield 11. "Count per user" isn't expressible as a DB constraint, overshooting by a handful is negligible against the vector, and a transaction here would serialize an ordinary user action. The per-user daily quotas (S4) are the tight bound.

### A4c — fortune free-tier scoping ✅ verified (and it reshapes A4a's rationale)
`DailyFortuneSnapshot` is `@@unique([chartHash, scope, anchorDate])`, and **`chartHash` is derived from birth data only** — date, time, city, timezone, gender — with **no `userId` or `profileId`** (`fortune-snapshot.helpers.ts::computeChartHash`).

So the free tier is *one AI narration per distinct birth chart, per scope, per day*, **shared across users** — the same cross-user cache decision as F4, applied to fortune. That means profile *count* alone doesn't multiply spend; **distinct birth data** does. An attacker gets a fresh chart just by varying the date, so the vector is real — but the cap is what bounds how many distinct charts one account can register.

**Concrete bound after A4a**: 10 profiles × 3 scopes × 1/day = **≤30 AI narrations/day per free account** (≈$1–6/day), versus unbounded before. Combined with the routes' existing 10/min throttles.

### A4b — quota SPEC (build is S4, Phase 2)
Recorded here so S4 implements rather than re-decides:

| | |
|---|---|
| Key | `quota:{userId}:{yyyymmdd}` — **day boundary in Asia/Taipei**, matching `FORTUNE_DEFAULT_TZ` and the S2 spend ledger (a UTC key resets at 8am local and produces confusing support tickets) |
| Limits (env-tunable) | readings **20/day** · chat messages **200/day** · fortune narrations **30/day** — deliberately generous; these are anti-abuse ceilings, not product limits |
| Error | HTTP 429, `{ code: 'QUOTA_EXCEEDED' }`, zh-TW message |
| Ordering | Checked **before** credit deduction, so a refused request never needs a refund (mirrors the S2 rule) |
| Storage | Redis `INCR` + TTL, same util as the S2 spend counters |
| Relationship to other controls | A4a caps *breadth* (how many charts), quotas cap *rate* (calls/day), S1 caps *concurrency*, S2 caps *money*. Four different axes — none substitutes for another |

---

## A6 — refund / clawback idempotency ✅ verified, no live defect

Every credit-returning path is reachable more than once — the AI-failure handler and the client-disconnect handler can both fire for one chat message, RevenueCat redelivers webhooks, and a user can retry a failed reading. Each was re-read and each replay is a no-op.

| Path | Guard | Verdict |
|---|---|---|
| Reading refund (`CreditsService.refundReadingCredit`) | `updateMany WHERE refunded_at IS NULL AND credits_used > 0`, inside a tx | ✅ idempotent + race-safe |
| Comparison refund (`refundComparisonCredit`) | same guard, **plus** clears `paidAt` and nulls `aiInterpretation` | ✅ — and correctly revokes access, not just money |
| Chat message refund (`ChatPaymentService.refundLastMessage`) | `updateMany WHERE refunded_at IS NULL` on the message; both sub-branches guard `> 0` before decrementing | ✅ — all four call sites converge on this one helper |
| Chat tier-upgrade stranded refund (`refundStrandedPaidOnTierChange`) | optimistic-concurrency check on `(paidMessagesUsed, creditExtensions)`; sets `paidMessagesUsed = creditExtensions * 10` | ✅ — a second tier change computes `refundable = 0` and skips |
| RC consumable clawback (`handleCancellation`) | UNIQUE `Transaction.stripePaymentId = rc-refund:<txid>` created **before** the clawback; P2002 → skip | ✅ — DB-level, independent of the webhook's Redis dedup |
| RC consumable grant (`handleConsumablePurchase`) | same pattern with `rc-purchase:<txid>` | ✅ |
| `clawbackCredits` arithmetic | `GREATEST(credits - n, 0)` in one row-locked UPDATE, `LEAST` captures the true amount | ✅ can't go negative under concurrency |

Regression coverage: `test/refund-idempotency.spec.ts` (13). These drive the real guards against an in-memory store with conditional-update semantics, so a replay returns `refunded: false` because the service's own `WHERE` matched nothing.

> ⚠️ **The fake's reads return a snapshot, not the live row — that detail is load-bearing.** With a live reference, a racing caller's write retroactively changed what an earlier reader saw, so the cheap pre-read appeared to stop the double-refund and the atomic guard was never exercised: deleting the guard entirely still passed. Caught by mutation. Three mutations now produce specific failures — removing the reading guard fails the concurrency test, removing the message guard fails the converging-callers test, removing `paidAt: null` fails the access-revocation test.

### A6-adjacent: credit movements that bypassed `CreditLedger`

A sweep of every `credits: { increment | decrement }` and absolute `credits:` write turned up three sites outside the ledger. `sum(CreditLedger.amount)` is supposed to reconcile against `user.credits`; each of these silently broke that.

| Site | Status |
|---|---|
| `admin.service.ts::adjustUserCredits` | ✅ **FIXED** — see A7 |
| `ads.service.ts` CREDIT grant | ✅ **FIXED** — ledger row added inside the existing transaction. `AdRewardLog` records that an ad was watched; only `CreditLedger` reconciles. Path is disabled by `ADS_REWARDS_ENABLED=0`, so this is pre-work for whoever re-enables it after AdMob SSV |
| `zwds.service.ts` ×3 (`createReading` :404, `createComparison` :665, `createCrossSystemReading` :824) | ⚠️ **Left as-is, deliberately.** Unreachable — the ZWDS controller exposes only `GET readings/:id`; all three POST routes were removed, and this is already documented at `zwds.controller.ts:23`. Fixing them means re-testing a dead subsystem. **Anyone re-enabling those routes must route the spend through `CreditsService.deductCredits` first** |

---

## A7 — admin money ops ✅ FIXED

`PATCH /api/admin/users/:id/credits` mints or removes real value. Auth is sound (`AdminGuard` → Clerk `publicMetadata.role`, 60s cache, fail-closed; `ClerkAuthGuard` is a global `APP_GUARD` so `request.auth` is verified before it runs). Two defects in the handler itself:

**1. No `CreditLedger` row `[medium]`.** It wrote an `AdminAuditLog` entry but no ledger row — the only credit path that moved value invisibly to the ledger. Fixed by routing through a new `CreditsService.adjustCredits(userId, signedAmount, reason, { tx })`, which ledgers both directions. `CreditLedger` has no actor column and this branch adds no migrations, so the admin id is encoded in the reason: `admin_adjust:<adminUserId>:<note>`. Credit change + ledger row + audit log now share one interactive transaction — an audit entry claiming a grant that rolled back is worse than no entry.

**2. Lost update via absolute write `[medium]` — a real money bug.** It read `user.credits`, added the delta in JS, and wrote the **absolute** result. Any spend landing between the read and the write was erased: a user burning 3 credits while support granted +1 got the 3 back for free. Fixed — the negative branch is a guarded `updateMany WHERE credits >= n` (the `deductCredits` pattern), the positive branch a relative `increment`. Neither reads-then-writes. The friendly "would result in −N" pre-check is kept for the error message; the atomic guard is what actually enforces the floor.

**3. DTO guard rails `[low]`.** `reason` was `@IsString()` with no emptiness check, so `""` — and, after adding `@IsNotEmpty()`, `"   "` — produced an audit row explaining nothing; `amount` was an unbounded `@IsInt()`, so a slipped keystroke could mint 1,000,000 credits. Now: trimmed + non-empty reason, non-zero amount, `|amount| ≤ 10_000`. These are guard rails against a fat finger, not a trust boundary — the caller is already an authenticated admin, and a mistyped amount is far likelier than a malicious one.

Regression coverage: `test/admin-credit-ledger.spec.ts` (12), plus the rewritten `admin-service.spec.ts::adjustUserCredits` case. Mutation-verified: removing the ledger write fails 3 tests; reverting to the absolute read-modify-write fails 5, including the lost-update test specifically.

---

## Phase 1A gate — three-auditor line audit (completeness · correctness · test validity)

Run against the completed 1A. Findings below are **verified against source by hand**, not relayed.

### Fixed in this pass

| # | Finding | Where |
|---|---|---|
| F11 | `regenerateReading` zeroing `creditsUsed` on never-refunded rows — self-inflicted, HIGH | See the F2 revert above |
| F7 | **Monthly subscription credits bypassed `CreditLedger`** — the largest recurring inflow, invisible to reconciliation. The docblock said so outright ("preserves the pre-M6 behavior"). ⚠️ This site *was* in the A6-adjacent grep output; the sweep triaged three hits and stopped, and the doc's "turned up three sites" was wrong. Now ledgered inside the existing transaction | `entitlements.service.ts:193` |
| — | Stream refusal misdirected users to 「重新生成」, a path that always refuses for a refunded row (its comment was wrong for the same reachability reason as F11) | `bazi.service.ts:671` |
| — | `adjustCredits`' audit-log `before`/`after` could be arithmetically incoherent — separate reads under READ COMMITTED, so a spend in the window yields `before=10, after=8` for `amount=+1`. `before` is now DERIVED from the post-write value, so `after - before === amount` always | `credits.service.ts:216` |
| — | `startsWith` comment claimed to "anchor" the deleted-account match. Prisma compiles it to `LIKE` without escaping and `_` is a wildcard, so it narrows rather than anchors. Inert (fixed-length Clerk ids; fails toward withholding) — comment corrected rather than code | `signup-bonus.ts:68` |

### Test-validity fixes — 4 vacuous, 1 uncovered

29 mutations were applied across the seven security specs. 23 controls were genuinely locked. The rest:

| Control | Was | Now |
|---|---|---|
| RC constant-time comparison | **VACUOUS** — swapping `timingSafeEqual` for `===` failed nothing across all 41 RC tests. The test asserted two same-length wrong headers both return `false`, which `===` satisfies | Asserts the **mechanism**: `verifyAuthHeader.toString()` must contain `timingSafeEqual`. Timing is non-functional — no return-value assertion can observe it, and `jest.spyOn` can't patch non-configurable `node:crypto` exports. Mutation now fails |
| Comparison `aiInterpretation: Prisma.DbNull` | **VACUOUS** — a bare `.not.toEqual(...)` plus a fake that wrote `undefined` as if it were a value. **Real Prisma treats `undefined` as "don't update"**, so the realistic one-word bug ships a refunded comparison with its report fully readable, green | Fake now skips `undefined` like Prisma does; test asserts the exact `DbNull` sentinel. Mutation now fails |
| Admin lost-update | **PARTIAL** — passed with the absolute write once the mock's throwing oracle was relaxed. The concurrent spend was injected at the admin-level read, which `adjustCredits` doesn't reuse, so both implementations landed on 8 | Injects at the write statement; fake accepts both shapes so the **arithmetic** discriminates (8 vs 11), not the mock |
| Both Clerk-webhook signup sites | **UNCOVERED** — hardcoding both back to `credits: 3` passed the entire suite. F1's whole point is that there are three insert sites | Both handlers now driven directly; the mutation fails |
| Clawback `GREATEST(...,0)` floor | **VACUOUS and left so** — the fake computes `Math.min` itself, so deleting the entire clamp from the SQL passes. A mock cannot validate SQL | Renamed to what it actually locks (the service reports the DB's value, not the requested amount) with an explicit scope-limit note. **Real coverage needs an integration test against Postgres — not yet written** |
| `READING_REFUNDED` error-contract test | **TAUTOLOGY** — constructed the exception inline and asserted its own `code`; would pass with `bazi.service.ts` deleted. And no client branches on it: `streamReading` forwards only `err.message` | Deleted |

### Confirmed findings routed to later phases — NOT fixed here

Real and verified; each sits outside 1A's money-path scope. **F5 is the one to look at first.**

| # | Finding | Conf. | Phase |
|---|---|---|---|
| ~~**F5**~~ | ✅ **FIXED** — commit `dd304c0` + audit follow-ups. Full write-up in the F5 section below. | 92 | done |
| **F6** | **Chat can be opened on a refunded reading** — the sibling F2 missed. The reading branch selects `{id, userId, readingType}` (no `refundedAt`) and checks ownership only; the comparison branch 20 lines below gates on `paidAt` with a comment stating the exact principle. Chat rebuilds the analysis from the birth profile, so the refunded content is narrated anyway. Needs an AI failure to set up, hence not click-to-repeat | 80 | **1B** |
| **F8** | RC `PRODUCT_CHANGE` / `UNCANCELLATION` re-grants a full month of credits: the handler writes the event's new `purchased_at_ms` onto the subscription *before* granting, and the idempotency key is `[userId, currentPeriodStart]` — so the new key never collides. `PRODUCT_CHANGE`/`UNCANCELLATION` appear in zero tests | 80 | Payments slice — **before IAP ships** |
| **F10** | `handleConsumablePurchase` commits its idempotency `Transaction` row and then grants in a **separate** statement. A crash between them locks the customer out permanently — every retry hits P2002 and returns. The Stripe path already fixed exactly this (`stripe.service.ts:1053`); `grantCredits` already accepts a `tx` | 85 | Same slice |
| **F9** | `POST /api/payments/upgrade` blind-writes `Subscription.planTier` + `User.subscriptionTier` on Stripe-API success with `proration_behavior: 'create_prorations'` — no `invoice_now`, so entitlement precedes collection. Certain from the code regardless of billing config: it bypasses `syncUserTier`, and a user with Stripe BASIC + Apple MASTER who calls it is blind-downgraded despite an active Apple sub | 74 | **1B** |
| — | `deleteAccount` zeroes `credits` with no compensating ledger row (5th unledgered write). Harmless — the account is being retired — but it belongs with the deletion-cascade review | — | **1C** |
| — | ZWDS ×3 unledgered decrements — unreachable, documented above | — | on re-enable |

### Confirmed clean (auditable coverage)

Kill switches have exactly one entry point each, gate as the first statement, and no bypass exists (attempted and failed). No crons. ~~Billing precedes generation at all 18 Anthropic call sites.~~ **Overstated — corrected at the Phase 1 gate.** In `createReading` the order is generate → charge: the balance check at `bazi.service.ts:178` is a **stale read** of `user.credits`, the provider call runs at `:370`, and `deductCredits` only executes inside the `$transaction` at `:441`. The `reading:create:{userId}` lock serializes creates but not chat or comparison spends, so a concurrent spend between the pre-check and the deduct means we pay Anthropic and roll the transaction back — and the interpretation is written to the shared cache fire-and-forget at `:415`, *before* the deduct, so the retry is a free `fromCache` reading. Bounded and racy rather than exploitable on demand, and pre-existing; recorded because "billing precedes generation" is the sentence a future reviewer would trust instead of re-reading the function. All three webhooks verify before any state change and fail closed on an unset secret. No client-callable payment-confirmation route exists — payment state mutates only through the three secret-verified webhooks, and credit-package amounts are re-read server-side. All 105 routes across 16 controllers enumerated with guards; every `@Public()` route traced, none touches `aiInterpretation`, `DailyFortuneSnapshot`, or `ChatMessage`. No share-token, public-slug, or OG route in the API. `adjustCredits` is genuinely atomic and is **not** a nested interactive transaction.

One pre-existing inconsistency noted but not filed: `isSubscriber ||` short-circuits three paywalls, which on the comparison path would return a full report for a `paidAt: null` row — unreachable today because `refundComparisonCredit` clears `paidAt` and nulls the interpretation together, so it needs a legacy or partially-failed row. Worth closing for consistency; not a working exploit.

---

## F5 — fortune subscription window on the AI-chat door ✅ FIXED

**The hole.** The fortune HTTP routes enforce a window: FREE sees only the current period, paid tiers see −1 period back and +30 days / +12 months / +4 years forward. AI chat is a SECOND door onto the same engine output, and it validated profile ownership and the scope enum and nothing else. A FREE user created a session with `fortuneAnchorDate: "2030-01-01"`, spent 1 credit to extend, and had the AI narrate a 年運 that `GET /api/fortune/yearly?year=2030` refuses with 403. Worse commercially: a subscriber read **any** year at **zero** marginal cost, out of their monthly free-message quota rather than the +4yr window.

**Root cause is the shape, not the missing `if`.** The rule had one implementation reachable from one door. Patching only the chat call site would have left that shape intact.

**Fix.**
- `src/fortune/fortune-window.ts` (NEW) owns `FORTUNE_WINDOWS` + `assertFortuneWindow`. The three HTTP gates delegate to it; the six window constants in `fortune-snapshot.helpers.ts` are re-exports of that spec, so the *numbers* cannot drift either.
- Four chat doors enforce it: `createSession` (fail fast — no row), `sendMessage`, the SSE stream path, and `extendSession` (don't sell 10 messages every send will refuse — the same reasoning as the drift check beside it).
- `getChatContextForFortune` takes a **required** `userId` and lost its parameter defaults, so a call site that forgets the gate is a compile error. Both existing call sites were found that way. It runs before the profile lookup, cache read and engine call — a refused request costs nothing.
- Fails closed on a malformed anchor, in two layers: a per-scope shape regex, then a `Number.isFinite` backstop. Both are load-bearing — `2026-13-45` passes the DAY shape check and still yields `NaN`, and `NaN` satisfies neither `<` nor `>`.

**The per-message check is the load-bearing one.** The anchor is pinned on the session row, so a subscriber can open a +4yr session and then downgrade; gating only create would leave it serving content the tier no longer covers. Mirrors the comparison path's mid-session `paidAt` re-check.

**Behaviour change, bounded.** A FREE session spanning a period rollover now stops at the boundary rather than at the 24h session expiry — at most a few hours, and the content is exactly what the HTTP route refuses. Subscribers are unaffected (their −1 period past window covers it).

### Follow-ups from the F5 line audit (verdict: PARTIALLY CLOSED → now closed)

The auditor confirmed the hole was fixed, the arithmetic correct at every boundary, the HTTP refactor behaviour-preserving, and found no fifth door leaking new content. It also found that **the fix had failed its own premise**:

| # | Finding | Resolution |
|---|---|---|
| **F-1** | ⚠️ **The consolidated rule shipped with zero tests on half its callers.** All three HTTP gate bodies replaced with no-ops → the **full 1482-test suite passed**. That removes the paywall from six fortune doors with CI green — the same defect class as F5. The inverse was equally invisible: wiring the yearly gate to `'DAY'` compiles, 403s every yearly request, and passed all 184 fortune tests (a silent outage of a paid feature) | `test/fortune-http-window.spec.ts` — 17 tests pinning scope, tier and boundary per gate. Both mutations now fail specifically |
| **F-2** | The window refusal surfaced to the client as `AI_CALL_FAILED` / 503 «AI 暫時無法回答», with the message row stamped `AI_FAILED` — because the gate throws from inside the try block owned by the Anthropic-failure handler. No billing harm (the credit is refunded either way) but the frontend keys its paywall UI on `code`, and every refusal inflated the AI-failure rate that Sentry and the A6/A7 telemetry watch | Both message doors rethrow an `HttpException` with its real code, after refunding. Mutation-locked |
| **F-3a** | Chat gate timezone untested — swapping `nowIsoInTz(tz)` for `nowIsoInTz('UTC')` passed 39/39. Not cosmetic: it decides *which single calendar day* a FREE user may read, and for the 8h 16:00–23:59Z window a UTC clock denies today and grants yesterday | Test pins Taipei resolution on both sides |
| **F-3b** | The `Number.isFinite` backstop untested and mutually masked with the shape check | Test uses well-shaped-but-impossible dates (`2026-08-32`) that only the backstop catches |
| **F-4** | `"fails closed when the user row is missing"` was **vacuous** — the profile mock also returned null, so a *different* `NotFoundException` satisfied it with the gate deleted | Asserts the specific message and that the profile lookup was never reached |

### Known residual — not this fix's job

**The engine tier has no concept of the window** (confidence 75, pre-existing). `/build-chat-context-fortune`, `/daily-fortune`, `/monthly-fortune`, `/yearly-fortune` in `packages/bazi-engine/app/main.py` have no auth at all — the whole window rule is a NestJS-tier property, and the only production control is Railway private networking. An SSRF or a mis-set port binding bypasses F5 entirely. **This is exactly B3 (engine shared secret) in the Phase 1B plan** and should be treated as a hard prerequisite for launch, not an optional hardening.

---

## B1 — `@Public()` inventory ✅ + O3 server-side paywall ✅ FIXED

17 public routes across 10 controllers. Per-route justification:

| Route | Why public | Verdict |
|---|---|---|
| `GET /health` | liveness probe | ✅ |
| `GET /api/legal/{privacy,terms}` | published policy documents | ✅ |
| `GET /api/banner`, `GET /api/ads/config` | display config, no PII | ✅ |
| `GET /api/payments/{gateways,plans,credit-packages}` · `GET /api/bazi/{services,plans}` | price/product catalogue — the pricing page renders signed-out | ✅ |
| `GET /api/chat/sample-questions{,/all}` | static admin-authored prompt text, no chart data | ✅ |
| `POST /webhooks/{stripe,revenuecat,clerk}` | signature/secret-verified; auth is the signature, not the session | ✅ (A3) |
| `POST /api/bazi/calculate` | free chart preview — the acquisition funnel | ✅ engine passthrough, no AI, no PII returned beyond what was posted |
| `POST /api/bazi/explain-element` | click-to-explain on the free chart | ⚠️ **was leaking paid content — FIXED, see O3** |

### O3 — the paywall was client-side only `[medium]`

`explain-element` returns the element encyclopedia. The engine **deliberately emits every layer** (its docblock says so) and `ElementExplanation.tsx` hid the paid ones behind an `isSubscriber` prop. A client-side paywall is not a paywall — `curl` returned the paid tiers in full, unauthenticated.

Root cause was shared with a second defect: **`ClerkAuthGuard` returned `true` for a `@Public()` route without populating `request.auth`**, even when the caller sent a valid token. So a public route could not distinguish a subscriber from an anonymous visitor at all. (⚠️ An earlier revision of this section also credited the fix with repairing M1's rate-limit keying. **That was wrong** — no custom throttler tracker exists (`grep getTracker|extends ThrottlerGuard|generateKey` → 0 hits); `app.module.ts` registers the stock IP-scoped `ThrottlerGuard` and M1 is still future work. Optional auth is a *prerequisite* for M1's userId keying, not a delivery of it. Same overstatement class as F-2, which this very batch corrected.)

**Fixed:**
1. **Optional auth** — `ClerkAuthGuard.tryAttachAuth` verifies a Bearer token when present on a public route and attaches a **signature-verified** identity; a missing/expired/malformed token proceeds anonymously. It can never throw: that would turn a public route private.
2. **Server-side gate** — `stripPaidExplanationLayers` removes the `personalized` block (Layers B `pillarMeaning` / C `godRoleMeaning`+`godRole` / D `genderMeaning`) and `pillarContext.paid`, keeping Layer A and `pillarContext.free`. Boundary mirrors the component exactly.
3. **Fails closed** — absent id, unknown user, or DB error ⇒ free tier. A subscriber briefly seeing the free tier beats handing paid content to everyone on a hiccup.

16 tests. ⚠️ **A mutation found the gap that mattered — again**: the strip helper was tested, but making `isSubscriberByClerkId` return `true` unconditionally (the exact bug) passed the ENTIRE suite, because *the decision to call it* had no test. Same finding class as F6's stream door. Now covered anonymous / FREE / subscriber / fails-closed, and four mutations each fail specifically.

---

## B2 — ownership matrix (IDOR) ✅ CLEAN, no defects found

Every route taking a caller-supplied id, and how the row is scoped. Two patterns are in use and **both are sound**; the distinction matters when reading the code:

- **scoped-where** — `where: { id, userId: user.id }`, so a foreign id simply returns nothing
- **fetch-then-check** — `findUnique({ where: { id } })` followed by an explicit `!== user.id` → `ForbiddenException`

| Surface | Pattern | Verdict |
|---|---|---|
| `bazi.service.ts` readings + comparisons | scoped-where (`id + userId` ×11) | ✅ |
| `zwds.service.ts` readings, profiles, compat parties | scoped-where (×8) | ✅ |
| `users.service.ts` birth profiles | scoped-where (×3) | ✅ |
| `section-unlock.service.ts` (both payment routes) | scoped-where (`userId + readingId`) | ✅ |
| `fortune` snapshots | entry point resolves the profile `{ id, userId }`; the `chartHash` cache is then shared **by design** (F4 intended pricing) | ✅ — a caller can only obtain a chartHash for a profile they own |
| `chat.service.ts` sessions | fetch-then-check ×3 at entry points | ✅ |
| `chat-stream.service.ts` sessions | fetch-then-check ×1 at the entry point | ✅ |

**Unscoped lookups exist and are all safe — verified individually, not assumed.** ⚠️ *Two of the seven lines this section originally cited were wrong (B1/B2 audit finding 9): `bazi.service.ts:1597` is a `throw`, and `:1702` is the closing brace of an ALREADY-SCOPED `updateMany({ where: { id, userId } })`. The conclusion held; the evidence for it did not. Corrected list below.* `bazi.service.ts:1717`, `chat.service.ts:827/1078` and `chat-stream.service.ts:369/835` are **re-fetches of an already-authorized row** inside a method that validated ownership at entry (`generateComparisonAI:1631`, `recalculateComparison:1474`, `sendMessage:733`, `_streamWithLock:187`). Not entry points, not reachable with a foreign id.

All four suspects the plan named — fortune chartHash snapshots, chat session ops, comparison reveal, re-stream — were checked and are clean. The IDOR surface holds; the defects on this branch were entitlement checks (F2/F5/F6/O3), not ownership ones.

---

## F6 line audit — verdict was "closed in code, not in the safety net"

The four narration doors were correctly gated and no fifth narration path exists. What the audit found was the safety net, plus two adjacent entitlement defects.

| # | Finding | Conf. | Resolution |
|---|---|---|---|
| **F-1** | **The `sendMessage` door had NO test.** Deleting `assertReadingNotRefunded` from it passed the **entire 1530-test suite**. The F6 spec tested the HELPER, not that CALLER, and `chat-service.spec.ts`'s `beforeEach` defaults `baziReading.findUnique` to a non-refunded row — so its six `sendMessage` tests traversed the gate without exercising it. `POST /sessions/:id/messages-sync` is a live authenticated route, so a refactor dropping that line would have restored the F6 exploit with a green suite | 100 | ✅ Fixed — dedicated door tests; the audit's M3 mutation now fails |
| **F-4** | **`getReading`'s `isSubscriber \|\| isEntitled` defeated the refund gate**, removing exactly the coverage the comment above it claims to provide. Subscribers are NOT exempt from paying — `createReading` computes `creditsUsed = fromCache ? 0 : service.creditCost` with no tier branch — so a refunded subscriber is no more entitled than a free user. Pre-existing (F2 era), stale from an all-access model | 80 | ✅ Fixed in `bazi.service.ts` **and** the `zwds.service.ts` twin |
| **F-5** | `resolveOwnedCrossSellTargets` counted refunded readings as owned, so a refunded user is told 「您已解鎖…」 instead of being offered the reading. No content leak — a suppressed upsell | 90 | ✅ Fixed — `refundedAt: null` added to both queries |
| **F-2** | Claim 6's comment said an always-fire gate "would break every one" of the 16 stream tests and was "verified by mutation". Measured: **8 of 16** (the rest short-circuit earlier). The mutation was run; the count was not | 95 | ✅ Comment corrected, including a note that the control can rot silently via the `beforeEach` default |
| **F-3** | **Chat transcripts survive the refund.** `getMessages` and `listSessionsForReading` check ownership only, so a narration delivered before the refund stays re-readable for the 12-month retention window. Sequence: create → open chat *while generation is in flight* → extend → 10 questions → generation fails → refund. Net 1 credit, full analysis delivered | 85 | ⚠️ **Accepted, not fixed — see below** |

**Why F-3 is accepted rather than fixed.** F6 stops the *next* message; it does not revoke what was already narrated. Three reasons to leave it: the attacker cannot force the AI failure that triggers the refund, so it is not a repeatable vector; `assertComparisonUnlocked` has the identical gap, making this consistent existing design rather than a regression; and retroactively blanking a transcript the user legitimately received is a product decision, not a security one. **Revisit if refunds ever become user-initiated** — that would turn an unforceable race into a repeatable exploit.

### The lesson F-1 carries

The F6 commit message claimed the "which callers have NO test?" question had been asked deliberately at the start of 1B. It had — and it was answered for the stream door, then never re-asked for the sibling caller **twenty lines away in the same file**. Asking it once per file is not enough; it has to be asked **per caller**. This is now the third instance in this phase of a control whose *wiring* was untested while its *helper* was well covered (F6 stream door, O3's tier decision, F-1's `sendMessage`).

---

## B1/B2 line audit — verdict: B2 clean, B1 sound, **O3 must not merge as-is**

B2 was re-enumerated independently (91 routes / 15 controllers, including body-nested, query-param and hash ids) and confirmed clean. B1's security properties are pinned by mutation — the identity is genuinely signature-verified, and a public route cannot be turned private. **O3 was incomplete in three independent ways and broke its only live client.**

| # | Finding | Conf. | Resolution |
|---|---|---|---|
| **1** | **The fix never touched the web surface.** Web calls `/api/explain-element` — its own Next.js route, which proxied the **engine directly**, bypassing NestJS and therefore the gate. Only mobile used the route that was fixed. The original `curl` exploit was unchanged on web, while the audit doc recorded O3 as FIXED | 95 | ✅ Web route rerouted through NestJS (M10's reroute for this one route, early), **minting the bearer server-side** — the browser client sends no Authorization header, so forwarding alone would have served every subscriber the free tier |
| **2** | **`interactions` is subscriber-only on both clients and was not stripped** — and the new suite ACTIVELY REJECTED the fix: a test under the heading "keeps the free tier intact" asserted `interactions` was kept, so it would have told the next engineer they had broken the free tier | 92 | ✅ Stripped; the assertion corrected |
| **3** | **`dayPillarCombo` paid subfields not stripped** (`summary`/`gradeReason`/`specialLabels`/`lifeStageSeat`; `grade`+`teaser` are the free teaser behind an unlock CTA). The engine ships all 60 combos, so an anonymous caller could enumerate the entire paid set. Untested in both directions — the fixture omitted the field | 90 | ✅ Stripped to `{grade, teaser}`; fixture rebuilt against the REAL engine shape (`generic`, not the invented `layerA`/`title`) |
| **4** | **The strip crashed the only client of the route it protects.** Mobile dereferences `data.personalized.pillarMeaning` unguarded, and its own error stub always supplies `personalized: {}` — "always present" is an invariant of this payload. `delete out.personalized` ⇒ `TypeError` for every non-subscriber mobile caller | 88 | ✅ Emptied (`= {}`) instead of deleted |
| **5** | **The M1 rate-limit justification describes a mechanism that does not exist** — repeated in the commit message, the guard comment, this doc and the test docblock. No custom tracker exists; the stock IP-scoped `ThrottlerGuard` is registered and M1 is future work | 90 | ✅ Corrected in all four places |
| **6** | `getComparison` kept the exact `isSubscriber \|\|` pattern F-4 removed, one screen away. Refunds are covered there (atomic `paidAt` + `aiInterpretation` clear), but the state `:922-924` names — "an unpaid row with a stale interpretation" — was served free to subscribers | 78 | ✅ Fixed + 3 tests |
| **7** | **The ZWDS half of F-4 had ZERO coverage** — restoring the bug passed the entire 1534-test suite. All five existing `getReading` tests mock `refundedAt: null` | 95 | ✅ Test added; the mutation now fails |
| **8** | O3's controller wiring untested (dropping `auth?.userId` passes). Fails SAFE — everyone resolves to the free tier — so a coverage gap, not a leak | 75 | ⚠️ Accepted; the service-level decision is covered and the failure direction is safe |
| **9** | Two of seven cited B2 line numbers were not lookups | 85 | ✅ Corrected above |
| **10** | `POST /api/bazi/calculate` forwards `reading_type` verbatim, so an anonymous caller can request the deterministic **pre-analysis** (narrative anchors, 用神 cascade, luck-period scoring) the free web preview deliberately omits. Not the paid artifact — the engine IP, third in the threat model | 72 | ⚠️ Recorded, not fixed — see below |

**#10 deferred deliberately.** Gating it means deciding what the free preview may compute, which is a product question, and the same content is already reachable through the engine directly until **B3-b** closes that. Folding it into B3-b's scope avoids fixing the same exposure twice. **Revisit when B3-b lands.**

### The recurring lesson, now five for five (see also the F9 line audit below)

Findings 7 and 8 are the same shape as F6's stream door, O3's tier decision, and F-1's `sendMessage`: a well-covered helper behind untested wiring. Asking "which callers have no test?" **once per file** has now failed four times; it has to be asked **per call site**, and the answer verified by mutation rather than by reading.

---

## F9 — `POST /api/payments/upgrade` granted tier on its own say-so ✅ FIXED

Two defects, both certain from the code and both fixed in `StripeService.upgradeSubscription`.

**1. Cross-provider blind downgrade.** It wrote `User.subscriptionTier = <requested tier>` directly, bypassing `syncUserTier`. That column is a cross-provider **maximum**, not this subscription's tier: a user holding Stripe BASIC + Apple MASTER who upgraded Stripe to PRO had MASTER clobbered with PRO. The webhook would eventually repair it via `syncUserTier` — but only if it arrives, and the user is under-entitled until it does. The raw write also skipped the chat-quota resnapshot that `syncUserTier` performs, so the chat cap lagged the tier. → now calls `entitlements.syncUserTier(user.id)` and returns its result.

**2. Entitlement written on "the API call didn't throw".** The local `Subscription` row is selected on **our** `status` column, which can be stale (unprocessed failed invoice, dropped webhook). If Stripe actually held the subscription `past_due` / `unpaid` / `incomplete`, the old code still wrote `status: 'ACTIVE'` + the higher tier — upgrading a delinquent account. → the response from `subscriptions.update` is now the authority: `mapStripeStatus(updated.status) !== 'ACTIVE'` ⇒ withhold the local grant, alert (`stripe.upgrade_not_entitled`), and return **402 `UPGRADE_PAYMENT_REQUIRED`**. Nothing is written; the `planSlug` metadata we just set means `customer.subscription.updated` lands the correct tier by itself once the invoice settles. Unknown statuses fail closed — `mapStripeStatus` returns `null`, which is not `'ACTIVE'`.

**Response shape** gained `effectiveTier` alongside `newTier`; they legitimately differ in the cross-provider case. The web client was asserting the *requested* tier locally (`setCurrentTier(planKey.toUpperCase())`) and now renders `effectiveTier`.

### ⚠️ ANTI-REMEDY — do **not** switch to `proration_behavior: 'always_invoice'`

The finding was phrased as "no `invoice_now`, so entitlement precedes collection", and the obvious tightening is to invoice immediately. **That is worse than the bug.** `always_invoice` mints a proration invoice whose `lines.data[0].period.start` is the moment of the change; `handleInvoicePaid` keys the monthly credit grant on exactly that value; `MonthlyCreditsLog.@@unique([userId, periodStart])` is a full `DateTime`. So every upgrade would collide with nothing and grant **a whole month of credits** — and upgrade → downgrade → upgrade nets ~zero in prorations while minting credits without limit.

Deferred collection is therefore deliberate and is the lesser exposure: an upgrade grants the new tier up to one billing cycle before payment, bounded by `handleInvoiceFailed` → `PAST_DUE` → `syncUserTier`. Pinned by a test asserting `proration_behavior: 'create_prorations'`, and by a comment at the call site. **Revisit trigger**: if immediate collection is ever wanted, the credit-grant keying must be reworked first (key on the *subscription's* period, not `lines.data[0]`).

Also noted while verifying, **not fixed** (low severity, pre-existing, not reachable for double-grant): `handleInvoicePaid` takes `lines.data[0]` blindly. A renewal invoice carrying mid-cycle prorations may put a proration line first, keying that cycle's `MonthlyCreditsLog` row to a mid-cycle timestamp. Impact today is a mis-keyed log row, not a double grant — one invoice per cycle means one grant per cycle, and redelivery still dedups. It becomes severe only under the anti-remedy above.

### Coverage

`test/subscription-upgrade.spec.ts` (20) + 3 route tests in `payments-controller.spec.ts`. Mutation-verified — each gate neutered with a **compiling** change:

| Mutation | Result |
|---|---|
| `syncUserTier` → blind `user.update` | 3 fail ✅ |
| ignore Stripe's status (`mapStripeStatus('active')`) | 9 fail ✅ |
| `cancelledAt` hardcoded `null` | 1 fail ✅ |
| `proration_behavior: 'always_invoice'` | 1 fail ✅ |
| `effectiveTier` = requested tier | 1 fail ✅ |
| `status: stripeStatus` → literal `'ACTIVE'` | **equivalent mutant** — past the gate `stripeStatus` is provably `'ACTIVE'`, so no test can distinguish. Kept for expressiveness, not claimed as covered. |
| controller passes a hardcoded user id (IDOR shape) | 2 fail ✅ |

**The per-call-site check paid off again.** `POST /api/payments/upgrade` — the one endpoint that can move a paid tier by request — had **no controller test at all** among the 14 covered payments routes. The service fix would have shipped behind wiring that no test touched, for the fifth time running. Conversely the sibling sweep came back clean: `grep 'subscriptionTier:'` across `src/` shows exactly two computed writes, `syncUserTier` and this one.

---

## F9 line audit — verdict: fix was sound, but under-scoped and self-refuting in one comment

Three auditors (backend correctness · test validity · blast radius). The change did what it claimed; almost everything they found was **outside the diff** or **in my own verification**.

### The sweep I ran was shaped wrong

I reported "the sibling sweep came back clean — `grep 'subscriptionTier:'` shows exactly two computed writes." That grep **cannot find the siblings that matter**. The input to `computeEffectiveTier` is `Subscription.status`, not `User.subscriptionTier`; sweeping *that* column finds `cancelSubscription` and `reactivateSubscription` — 20 and 60 lines from the function I was fixing — both writing status and neither recomputing the tier. Reactivate is the sharper one: the row goes ACTIVE, `User.subscriptionTier` stays FREE, every gate denies a paying customer, and the endpoint returns `{success: true}`. Self-healing via webhook, so a window rather than a permanent state — but it is the F9 defect, twice, in the same file.

**The lesson, sharpened: the per-call-site question is only as good as the column you ask it about.** Sweep the field the decision actually reads, not the field the bug was reported against.

### Fixed

| # | Source | Fix |
|---|---|---|
| 1 | blast, conf 95 | `syncUserTier` added to `cancelSubscription` + `reactivateSubscription` |
| 2 | corr, conf 85 | **Entitlement gate moved BEFORE `subscriptions.update`.** Refusing afterwards left Stripe re-priced *and* — via `cancel_at_period_end: false` — silently un-cancelled, with no rollback: schedule a cancellation, fail a renewal, attempt an upgrade, and Stripe ends up holding a higher price with the cancellation revoked while the response mentions neither. `stripeSub` from the retrieve already answers the question, and `create_prorations` raises no invoice so the update cannot move the status. Post-update backstop kept. |
| 3 | corr, conf 88 | **`pause_collection` now refuses.** My comment claimed `mapStripeStatus` "encodes exactly the entitlement question"; the same file rebuts it — pausing collection *leaves the status unchanged*, so a paused subscription reports `active`, passed the gate, and got the tier while Stripe collected nothing. Comment corrected, condition added. |
| 4 | corr, conf 80 / blast F-5 | On refusal the **stale local status is reconciled** to Stripe's (when mappable) and the tier recomputed. Writing nothing left our row asserting ACTIVE against Stripe's `past_due`, and left `handleInvoicePaid`'s dunning recovery — gated on a local `PAST_DUE` — unable to fire. The tier is still withheld; that is the grant being refused. |
| 5 | corr, conf 78 | `internalUserId` written into the update metadata. `handleSubscriptionUpdated` early-returns without it, so for any subscription not created by our checkout the 402's entire "the webhook will land it later" argument was dead. |
| 6 | blast F-2 / corr #75 | **`handleSubscriptionUpdated` no longer defaults an unmappable slug to a tier.** It was `planSlugToTier(planSlug \|\| 'basic')`, and that helper answers `'FREE'` for anything unrecognised — so a subscription whose metadata we don't own had BASIC or FREE written over its real tier on *every* event. New `knownPlanSlugToTier` returns `null`; the update omits `planTier` (as it already does for an unrecognised status) and the create path refuses to invent one. |
| 7 | corr #75 | `@IsIn(['basic','pro','master'])` on the DTO. `plan.findFirst` accepted any active slug while `planSlugToTier` answered FREE for a 4th plan — charged in Stripe, entitled to nothing. |
| 8 | blast F-6 / corr #68, #72 | `@Throttle(5/min)` on the route. It mutates Stripe before it can fail, its 402 literally invites a retry (「再試一次」), and it now reaches `refundStrandedPaidOnTierChange`, which **increments credits** — a path that previously required a webhook. Global was 100/min/IP; siblings use 5/min. |
| 9 | blast F-4 | Web `apiFetch` attaches `code` + `status` to the thrown Error. The discriminator I added was unreachable client-side — only `message` survived the wrapper. (The Chinese copy *did* render correctly.) Additive; nothing reads it yet. |

### Test defects found in my own tests

- **`does not write PRO`** asserted "no tier write at all", passing only by fixture accident (stored tier already MASTER ⇒ `changed` is false). Making that write unconditional-and-idempotent — still correct — turned it red. Narrowed to "no tier other than the cross-provider maximum", and the realistic **repair** case added (stored BASIC, Apple MASTER active ⇒ writes MASTER), which the old assertion would have failed.
- **The mock severed read-from-write.** `findMany` returned the post-state unconditionally, so `effectiveTier` was fixture-supplied: writing the *wrong* tier to the row, and **moving `syncUserTier` above the row write**, both stayed green. Replaced with a stateful fake that honours `where.userId`/`where.status` and reflects `update`. The ordering `syncUserTier`'s own docblock requires is now observable.
- **`it.each` rejections used a bare `.toThrow(HttpException)`** — which a 400 satisfies. Typed to 402 + code.
- **The metadata test was vacuous**: the fixture already carried `internalUserId`, so `...stripeSub.metadata` supplied it by spread and dropping the fix survived all 161 tests. Fixture emptied — which is also the realistic case.
- **A pre-existing test encoded the defect**: "should default unknown slugs to FREE". Rewritten to assert withholding.
- Anti-remedy claim (c) was **stated as fact on an unverifiable premise**. Every link is verified in-repo, but "a proration line's `period.start` is the moment of change" is Stripe runtime behaviour with no fixture here, and my only test asserted the literal string `'create_prorations'`. Split honestly: `monthly-credits.spec.ts` now pins **our** half — a proration-shaped invoice grants a full unprorated month, and two in one cycle grant twice — and the Stripe-side premise is labelled an assumption.

### Mutation results (all compiling)

pre-flight gate removed → 12 fail · `pause_collection` ignored → 1 · no reconciliation → 1 · `internalUserId` dropped → 1 (after the fixture fix; **survived before it**) · cancel loses sync → 1 · reactivate loses sync → 1 · webhook re-defaults slug → 5. Two attempted mutations failed to compile and proved nothing (`Tests: 0 total`); one of those — writing a `null` status — is prevented by the type system, which is a stronger guarantee than a test.

### The anti-remedy is reachable from the Dashboard, with no code change — now guarded

Checking the portal config (below) turned up something the code review could not: the credit-minting hazard my `create_prorations` comment warns about **does not require anyone to edit that line**. Stripe's portal has **Subscriptions → Charge timing → "Invoice prorations immediately"**. Flip that dropdown and every portal plan switch raises exactly the invoice the comment describes — proration line period starting at the moment of change, colliding with no `MonthlyCreditsLog` key, paying out a full unprorated month.

A comment on a line in `upgradeSubscription` protects nothing against a Dashboard toggle. So the invariant now lives in code: `handleInvoicePaid` refuses to grant when `billing_reason === 'subscription_update'`.

**Deny-list, not allow-list, deliberately.** `billing_reason` is absent from older payloads and from several of our own fixtures; an allow-list would silently stop granting to paying customers — verified, that mutation breaks 6 tests. Denying the one reason known to be wrong fails OPEN (users keep their credits) while closing the hazard, and anything unrecognised is granted **and** alerted (`stripe.unexpected_invoice_billing_reason`) so we learn rather than guess.

Mutations: guard disabled → 2 fail · alert suppressed → 1 · allow-list instead of deny-list → 6. The two "damage model" tests (a proration-shaped invoice grants a full month; two in one cycle grant twice) now run the deny-list around themselves, so they document the hazard while the guard test pins the defence.

### Portal configuration — checked 2026-08-14 (SANDBOX; live still unverified)

| Setting | Sandbox value | Consequence |
|---|---|---|
| Customers can switch plans | **ON** | F-2 is reachable in at least one environment. Treat as reachable in live until proven otherwise. |
| Charge timing | Invoice prorations at **end of billing period** | Matches our `create_prorations` — the credit-minting path is NOT currently triggered via the portal. Now moot: the code guard above holds regardless. |
| When customers change plans | Prorate charges and credits | Same semantics as our own call. |
| Downgrades | Update immediately | — |
| Promotion codes | OFF | — |

⚠️ **The sandbox switch list offers products that are not ours**: "Lite" $19/$190 and "Pro" $59/$590, against seeded plans 基礎版 $4.99/$39.99, 專業版 $9.99/$79.99, 大師版 $19.99/$159.99. That is Stripe's sample catalog, not this app's — so the config was never set up deliberately. A customer switching to one of those would land on a price with **no corresponding `Plan` row at all**, well beyond the stale-metadata problem.

**Still needed from the owner**: the same page in LIVE mode (`dashboard.stripe.com/settings/billing/portal`, no `/test/`). Sandbox and live portal configurations are independent.

### Accepted, NOT fixed

- **Portal plan switches land a stale tier** (blast F-2, conf 90). `handleSubscriptionUpdated` derives `planTier` from *our* `metadata.planSlug`, which the portal never updates — so a portal PRO→MASTER switch writes PRO. A proper fix needs a price→plan mapping we don't have (checkout mints ad-hoc `price_data`, not reusable Price objects); doing it properly means real per-plan Stripe Price objects, which is its own piece of work. **Sandbox has plan switching ON**, so this is not hypothetical. Interim options, cheapest first: (a) turn plan switching OFF in the live portal — the app has its own upgrade UI, so the portal doesn't need it; (b) restrict the portal's eligible products to ours; (c) the price→plan mapping. The silent-downgrade half is fixed above; this half is not.
- **Out-of-order webhooks can regress a successful upgrade** (corr #70). No ordering guard anywhere in the webhook handlers; equal to pre-fix. Needs a cross-cutting `event.created` comparison — its own piece of work.
- **Invoices name the wrong plan** (corr #60). The update reuses the *old* plan's Stripe product, so a BASIC→MASTER upgrade attaches the MASTER price to a product named 基礎版. Customer-visible on invoices; **revisit before real users receive any**.
- **A new Stripe Price (sometimes Product) per call** (corr #68). Account pollution, not theft, under `create_prorations`. The throttle bounds it.
- **Pricing-page cosmetics** (blast F-3, F-8): after a cross-provider change the tier badge correctly doesn't move while the toast still says 「方案升級成功！」; the error banner can render above the fold. Both cosmetic, and the cross-provider case is largely unreachable from the web UI anyway (a MASTER user sees no upgrade affordance).
- **`syncUserTier` is read-compute-write with no lock** (corr #82, pre-existing). Interleaving with `handleInvoiceFailed` can strand `User=PRO` against a `PAST_DUE` row.

---

## B5 — Clerk `authorizedParties` (azp allowlist) ✅ SHIPPED, ⚠️ INERT UNTIL CONFIGURED

`verifyToken` was called with `{ secretKey }` only, so this API accepted a token minted for **any** frontend origin on the same Clerk instance. `azp` is the claim Clerk sets to the requesting origin; it is inside the signature, so a bearer cannot edit it. Both call sites now go through a shared `verifyOptions()` — the protected path *and* the optional-auth path B1 added, which is exactly as exposed since `explain-element` hands out paid layers on the strength of that identity.

Config: `CLERK_AUTHORIZED_PARTIES`, comma-separated, Joi-registered, empty by default. **Kept separate from `CORS_ORIGINS` deliberately** — CORS is a browser-enforced hint about who may read a response; azp is a server-enforced claim about who a token was issued to. They also diverge in practice (a token-minting origin that never makes a browser fetch needs one and not the other).

### The boundary, verified in `@clerk/backend` rather than assumed

```js
assertAuthorizedPartiesClaim = (azp, authorizedParties) => {
  if (!azp || !authorizedParties || authorizedParties.length === 0) return;
  if (!authorizedParties.includes(azp)) throw ...
}
```

A token with **no** `azp` short-circuits before the allowlist is consulted. That is deliberate on Clerk's part and is what keeps **native clients working** — the mobile app has no web origin, so its tokens carry no `azp`. Two consequences, both pinned by tests:

- This is a constraint on tokens that *carry* an origin, **not a complete origin lock**. Do not read it as one.
- The obvious "hardening" — requiring `azp` — would **401 every mobile user**. A test asserts a no-azp token is accepted while an allowlist is configured, so that change fails loudly.

### Behaviour

| Route | Foreign azp | No azp | Allowlisted azp |
|---|---|---|---|
| Protected | 401 | accepted | accepted |
| `@Public()` | **anonymous, never 401** — a public route must not become private | accepted | accepted, identity attached |

### Parsing NORMALISES, because the matcher does not

Clerk compares with `includes(azp)` — exact and case-sensitive — against a lowercase origin with no trailing slash. So `https://App.Example.com/` matches nothing, and because a non-empty allowlist **is** enforced, that means **every web session 401ing at once**. The first version of this fix guarded only the harmless case (a stray `''` from a trailing comma, inert while a real entry matches) and left the harmful one to a sentence in this document. Prose is not a control.

The parser now lowercases and strips trailing slashes — safe, since scheme and host are case-insensitive per the URL spec and an origin never carries a path — and **reports every entry it had to rewrite** as a boot warning, so a malformed env var announces itself instead of being silently tolerated.

### The second verifier the first sweep missed

`apps/web/middleware.ts` runs `clerkMiddleware()`, which does its own `verifyToken` **and accepts a bearer header, not just the session cookie**. It had no allowlist, so the two halves of the app disagreed on what a valid token is. Now passes the same variable. Blast radius was smaller than the API's — a foreign-origin token buys page *shells*, since every data path goes to the API — but "the other half is stricter" is not a control.

### ⚠️ It does nothing until the env var is set

Empty means "no check", and the guard logs a `warn` at boot saying so. Deliberate — failing startup would take the API down over a control that was never configured — but it means **shipping this changed nothing on its own**.

**Launch gate**, before real users exist:

1. Set `CLERK_AUTHORIZED_PARTIES` on **both** the Railway API and web services (same value; they must not drift).
2. The value is the origin **exactly as Clerk emits it**: lowercase scheme + host, port when non-default, no trailing slash. Dev needs **both** `http://localhost:3000` and `http://127.0.0.1:3000` — CLAUDE.md documents 127.0.0.1 as the routine HSTS dodge, and `azp` differs per origin, so a single-value allowlist locks one out. **Not a bare host**: `@clerk/backend`'s own docs example shows `'api.example.com'`, which can never match an origin-shaped claim.
3. **Decode a real token from each platform rather than inferring** — web via `await window.Clerk.session.getToken()` in the console, and **mobile separately** (log the decoded payload in the dev client, or set the var in staging and watch one device request for `token-invalid-authorized-parties`). See the mobile caveat below.
   - ⚠️ **THREE minting surfaces, not two** (Phase 1 gate). O3's reroute added a **server-side** mint: `apps/web/app/api/explain-element/route.ts` calls `auth().getToken()` inside a Next.js route handler, because the browser sends no Authorization header on that path. If a server-minted `azp` differs from the browser-minted one, setting the allowlist drops every web subscriber to the **free explain-element tier** — silently, because both `tryAttachAuth` and the proxy swallow their own errors. That is the exact "misconfigured allowlist degrades subscribers silently" failure this section warns about, on the exact route it names. Decode this token too.
4. Also on the B4-C prod-Clerk cutover checklist in `~/.claude/plans/launch-scalability-security-plan.md`, since the origin changes with a custom domain.

Not set locally on purpose: the value is unverified for this dev instance and a wrong entry would 401 the owner's own browser session. The boot warning is the intended local state.

### ⚠️ The mobile claim is INFERENCE, not verified fact

An earlier version of this section stated flatly that mobile tokens carry no `azp`. That is not verifiable from source — **`azp` is minted by Clerk's FAPI servers**, not by anything in this repo. The circumstantial evidence is strong (the Expo SDK flags every request `_is_native=1` with `credentials: 'omit'`, no Clerk client package sets an `Origin` header, and there are zero `getToken({template})` calls anywhere, so all tokens are stock session JWTs), and `clerk-azp-contract.spec.ts` proves Clerk **accepts** a no-azp token while an allowlist is set. What is *not* proven is that a native token actually lacks the claim.

If that inference is wrong, setting the env var **401s every mobile user on every protected route**, including paid IAP entitlement checks. Mobile shares the same Clerk instance (`apps/mobile/.env.example`). Hence step 3 above — the original recipe only covered web.

### A misconfigured allowlist degrades subscribers SILENTLY

On a protected route a bad value 401s loudly. On a public route it does not: `tryAttachAuth` swallows everything by design, so a subscriber is quietly dropped to anonymous — and `explain-element` is exactly the route that decides whether paid layers are returned. The web proxy swallows its own errors too, so the symptom would be "customers say the paid content vanished" with nothing in any log. The guard now warns on **that one reason only** (`token-invalid-authorized-parties`, matched on the error's `reason` tag rather than its prose), leaving ordinary expiry silent.

### Coverage

Two specs, deliberately split by what they can prove:

- **`test/clerk-azp-contract.spec.ts` (15) — Clerk's REAL verifier.** Mints RS256 tokens locally with `signJwt` and runs `verifyJwt`; no network, no mock. Proves: a foreign azp is rejected with the expected reason tag · **a no-azp token is accepted while an allowlist is set** (the mobile claim, now evidence) · omission ≡ `[]` · and that trailing-slash, uppercase and bare-host entries all match nothing — then that our parser's output admits the real token in every one of those spellings. It ties OUR parser to THEIR matcher, which is the seam where these bugs live.
- **`test/clerk-authorized-parties.spec.ts` (26) — our wiring.** Parser cases, both call sites, boot warnings.

**The audit's killer mutation.** Reverting B5 entirely (`verifyOptions()` returns `{secretKey}` only) originally left **13 of 15 tests green — including all three with "azp" in the title**, because `verifyToken` was mocked and the "rejection" tests only proved the guard translates a rejected promise. Swapping the rejection for `new Error('potato')` kept them passing. Those tests are now named for what they check (error translation), the mobile one carries an explicit scope note, and the real claims moved to the contract spec.

Current mutation results: full revert → 2 fail (the two wiring tests, now accurately named — the honest number, since nothing else *should* detect it) · **drop normalisation → 10 fail** · delete the boot warning → 1 · remove the azp-specific public-route warning → 1 · optional-auth path drops the allowlist → 2 · protected path drops it → 2 · parser stops trimming → 1 · stops dropping empties → 2 · always send the list → 2.

Also fixed from the audit: the drift test **passed when the two paths had maximally drifted** (with the public path reverted to `return true` it re-read the protected call and compared it to itself) — it now asserts the call count advanced; three exact-match `toEqual`s were loosened to `toMatchObject` with one deliberate canary, because adding a legitimate option like `clockSkewInMs` failed four tests for reasons unrelated to their names; and the count in this section was **overstated as 17 when the file had 15**.

**A fixture caught in passing**: `public-route-optional-auth.spec.ts`'s config stub returned `'sk_test_fake'` for *every* key, which stopped being harmless the moment the guard read a second one — B5 would have been handed the secret key as its azp allowlist. Now key-aware. A blanket config stub answers questions it was never asked.

### Noted, not fixed

- **Dev-instance account-portal origin** (conf 60): sign-in happens at `engaged-fowl-56.accounts.dev`; whether any token reaching the API is minted by that origin rather than the app origin is unestablished. Cheap to settle with the same decode, and it only matters for the dev value.
- Removed while here: a `publishableKey` field the guard assigned and never read. A dead config read in an auth guard invites the assumption that something validates it.

---

## Owner answers — 2026-08-15 (resolves P0.3, P0.5, B4-O8 and the F-2 portal question)

### The engine is PRIVATE-ONLY. This materially de-risks B3.

Railway → engine → Settings → Networking shows **no public domain** — only the `Generate Domain` / `Custom Domain` / `TCP Proxy` buttons, with nothing generated. Private networking is live at `engine.railway.internal`.

**P0.5 is answered: there is no public domain on the engine**, so the "certificate-transparency logs make it discoverable" concern doesn't apply — there is no certificate. The engine is reachable only from inside this Railway project/environment, which is `bazi-app`, Postgres and Redis.

That reframes B3. The finding was "the engine has no authentication at all" — still literally true (`grep -c 'Depends|Header(|Authorization'` over `main.py` → **0**), but the reachable-attacker set collapses from *the internet* to *code already running inside your Railway project*. B3 becomes defense-in-depth against a compromised API, not a hole anyone can walk through today. **Re-rate: not urgent; still worth doing before launch.**

### No healthcheck is configured — which unblocks B3-b

Settings → Healthcheck Path shows the `+ Healthcheck Path` button, i.e. **none set**. This was the specific thing I said would break the deploy if I locked the engine blind. It cannot: Railway isn't making an HTTP healthcheck call, so adding auth cannot fail one.

The engine *does* expose `/health` (`main.py:253`), it just isn't wired to Railway. **If a healthcheck path is ever added, `/health` must be exempted from the B3 auth check** — otherwise every deploy fails at verification. Worth writing into B3's implementation, not discovering later.

### No Sentry DSN — B3-a(0)'s acceptance needs rewriting

Variables shows one service variable (`RAILWAY_DOCKERFILE_PATH`) plus 7 Railway-injected ones. **No `SENTRY_DSN`.**

B3-a(0) was "engine Sentry before B3-a, because the rejected-request counter needs somewhere to live." With no Sentry project for the engine, that acceptance is unsatisfiable as written. Two ways forward, and the second is now the better trade:

1. Create a Sentry project for the engine and set the DSN. Costs a signup step and brings the sentry-python PII settings into scope (`send_default_pii=False`, `max_request_body_size="never"`, **`include_local_variables=False`** — at an engine exception the locals *are* the birth data).
2. **Drop the Sentry dependency and count rejections in Railway logs instead.** The observe-then-enforce phase exists to prove no legitimate caller is being rejected; a log line proves that just as well at this scale. Given the engine is private-only and the only caller is our own API, the observation window can also be short.

**Recommended: option 2**, which removes the blocker entirely and lets B3 ship as one step instead of three.

### Stripe: live is not configured; test-mode plan switching is now OFF

The owner turned **"Customers can switch plans" OFF** in the sandbox, and reports live Stripe is not set up at all. The app runs on `sk_test`, so the sandbox config *is* the live behaviour today.

**F-2 (portal plan switches land a stale tier) is therefore unreachable in the only configured environment.** It stays recorded, not fixed, with a precise trigger: **the moment live Stripe is configured, check that page again** — live and sandbox portal configurations are independent, and the live default may well have switching on.

### B4 / O8: the zero-users reset is ACCEPTED

Owner confirmed: a production Clerk instance starting with no users is fine; the dev account and its 96 birth profiles do not need to survive the cutover. **B4's Phase 1 deliverable — the decision — is done.** Instance creation is deferred until a domain exists, since Clerk production requires DNS records on a domain you control and none is configured.

---

## B3-a — engine shared secret ✅ SHIPPED IN OBSERVE MODE (rejects nobody)

The finding was literal: `grep -cE 'Depends|Header\(|Authorization'` over `packages/bazi-engine/app/main.py` returned **0**. Anything that could route to the engine got a full chart, the compatibility analysis, and the chat context for any birth data it sent. Private networking was the only control.

**What shipped.** `packages/bazi-engine/app/engine_auth.py` — a pure-ASGI middleware that checks `X-Engine-Key` and, today, **rejects nothing**. It counts outcomes per `<path><-<caller>` into a periodic rollup on stdout (Railway logs). All 11 NestJS call sites go through a shared helper. (At the time of writing there was also one live Next.js route with its own copy of the helper; **M10 rerouted it through NestJS and deleted that copy**, so NestJS is now the engine's only client.) `ENGINE_REQUIRE_KEY=1` flips it to enforce; that flag is off and gated on the pre-flight below.

Design points that are load-bearing and non-obvious:

- **`/health` is exempt in both modes and excluded from the counter**, matched after stripping a trailing slash. Enforcing would otherwise fail every deploy the day a Railway healthcheck is added, and a continuous probe would hold the counter permanently non-zero so the gate could never pass. A human typing `/health/` into Railway's Healthcheck Path field reproduces exactly that, which is why the exact-match set was widened.
- **`absent` is counted apart from `invalid`.** A wrong key is a drifted service or someone probing; a missing key is a caller nobody keyed.
- **Real routes are protected from the path cap.** The middleware runs before the router, so 50 requests to invented 404 paths would otherwise fill the table and relabel every real endpoint `<other>` — blocking the flip indefinitely for the price of an unauthenticated scan.
- **Rejected-key fingerprints use Space-Saving, not a first-come table.** The question is "one drifted service resending a stale key, or a scanner spraying junk?", and a fixed table answers it backwards: whichever keys arrive first take the slots. Measured on the first implementation — a scanner sending 30 junk keys made a caller's 1000 repeated rejections *entirely invisible*. Counts become an upper bound once the table overflows; the payload says so.
- **Only `ENGINE_KEYS` is comma-separated.** `ENGINE_KEY` is taken whole, so a key containing a comma is not silently split into several shorter secrets that each authenticate.

### ⚠️ Sequencing hazard — set `ENGINE_KEY` BEFORE the observation window

With no key configured on the engine, the middleware never inspects the presented value, so a correctly-keyed caller and a completely unkeyed one both record `unconfigured` and `keyed` stays 0 forever. That window is **not evidence**. The rollup now logs it at WARNING and says so in the line itself, because the natural operator reading — "no warnings, so no unkeyed callers, safe to flip" — is exactly backwards: flipping in that state is a total outage, by design (unconfigured fails closed).

Order of operations: set `ENGINE_KEY` on the engine and the NestJS API → confirm `keyed` appears in the rollup → run the pre-flight → only then flip. (This said "both callers" when the web app held a key too; post-M10 there is exactly one caller, and the web service must not be given the key.)

### B3-b pre-flight exercise (referenced by `.env.example` and `engine_auth.py` — this is it)

With ~zero organic traffic pre-launch, none of the 9 non-health endpoints fires unless a human drives it, so "wait 24h and check the counter" is a gate that appears to need traffic that will never arrive — i.e. one that gets waived. Someone must drive all 9 as a seeded test user:

| Endpoint | How to reach it |
|---|---|
| `/calculate` | chart preview (also `bazi.reading` on the same path; `zwds.calculate` went with the ZWDS deletion, and `web.bazi-calculate` with M10 — the web app is no longer an engine caller) |
| `/explain-element` | click a cell in the chart |
| `/daily-fortune` · `/monthly-fortune` · `/yearly-fortune` | the three fortune tabs |
| `/compatibility` | create a comparison (3 credits) |
| `/build-chat-context` | **an actual chat message on a paid reading** |
| `/build-chat-context-compat` | an actual chat message on a `paidAt`-non-null comparison |
| `/build-chat-context-fortune` | an actual chat message on a fortune |

The three `build-chat-context*` builders are called only from `sendMessage` and `_streamWithLock` — never from `createSession` — so there is no free way to touch them. **Seed the paid reading and the paid comparison first**; discovering they don't exist on the day is the usual way this stalls. Cost ≈ 3–6 Anthropic calls, ~20 minutes.

**Flip conditions — all four:**
1. Every one of the 9 endpoints shows `keyed ≥ 1` (positive control).
2. Zero `absent` / `invalid` / `unconfigured` throughout the window (health excluded).
3. **Zero `engine-auth bookkeeping failed` lines.** A systematically broken counter reads "zero unkeyed" while passing everything through — the same "reads zero while non-zero" hazard the non-sampling requirement exists to prevent.
4. Window = exercise run + 1h settle. Longer only if a healthcheck or cron is later pointed at the engine.

⚠️ **`flush_counter` fires on graceful ASGI shutdown only.** A SIGKILLed or OOMed container loses its tail window. Do not read a redeploy-truncated window as a clean one.

### Residual gaps — recorded, not implied

- **The CI guard is a ratchet, not a proof.** `scripts/check-engine-callers.mjs` is a lexical scan. It catches raw `fetch`/`axios`/`got`/`undici`, URL-into-a-variable indirection, imports of a base URL from elsewhere (via route names), hardcoded `engine.railway.internal`/`:5001`, and property-accessed `fetch` — each pinned by a planted violation in `apps/api/test/engine-caller-guard.spec.ts`. It will not catch a URL assembled across two files through a renamed identifier, or a call through a fully dynamic property. **The first version caught only one of the four bypasses tried against it**, which is why the spec now plants twelve.
- **The correlation id is send-only.** `engineFetch` mints an `X-Request-Id` per call and the engine echoes it on a 401, but it appears nowhere in the rollup and NestJS does not log it, so an engine record cannot currently be tied back to an API request. The **caller name** is the working signal. The id is wired for the day the engine gets Sentry (Ob3); until then, do not plan on it.
- **The engine still has no general error visibility.** `configure_auth_logging` attaches a handler to `bazi_engine.auth` only — an unhandled exception in `/calculate` goes nowhere. This was B3-a(0)'s "second-order win" and it did not survive the collapse to one step. It is now unowned; **Ob3 and M3 both name B3-a(0) as a prerequisite and need re-scoping.**
- **Non-HTTP scopes pass through unchecked.** There are no websocket routes today (`grep '@app.websocket'` → 0). The day one is added it is unauthenticated *and* invisible to the counter.
- **`docker/docker-compose.yml` sets no engine key** and publishes the engine on `5000:5000`. Harmless in observe mode; a compose deploy that ever set `ENGINE_REQUIRE_KEY` would fail closed on everything.

### Corrections to the shipping commit (`2b3b230`)

Recorded rather than quietly fixed, because the overstatements were in the parts arguing why the control could be trusted:

- "Completeness is a property of the source" / "enforced by the CI guard" — overstated. It is a heuristic over identifiers and route names; see the residual gap above. The follow-up commit widens it and adds the bypass tests, but the honest claim is *ratchet*, not *proof*.
- "The guard's own spec plants ten distinct violations" — ten `it()` blocks, of which **six** planted violations and four asserted non-violations. Now 18 blocks, 12 planting.
- "engine 3120 pytest (72 new)" — the new file collected **74**.
- "Live uvicorn, enforcing: `/docs` 401" — accurate for that run, but it used a non-prod marker so docs were still mounted. In real production `/docs` is not mounted at all and returns **404**. The live check ran in a configuration production will never be in.
- "Flushed on shutdown so a scan-then-stop is still reported" — true only for a graceful shutdown.

---

## 🚦 PHASE 1 GATE — two-auditor whole-diff audit (money paths / authz + PII)

Run against the cumulative `main..HEAD` (28 commits, 95 files), not per-commit — every item was line-audited when it shipped. The gate looks for what per-item audits structurally cannot see: interactions between separately-reviewed fixes, one fix undermining another, and gaps that fell between slices because each auditor assumed the other covered it.

**Both halves returned GO-WITH-FOLLOWUPS. Nothing found was a live exploit or an active leak.** The findings below were verified against source before acting, and every fix was mutation-tested.

### Fixed at the gate

| # | Finding | Fix |
|---|---|---|
| **M-H1** | **A regression Phase 1 introduced.** The F9 sibling sweep added `syncUserTier` to `cancelSubscription` on the reasoning "any endpoint writing `Subscription.status` must recompute the tier" — true of the *webhook* handlers, false here. This is a `cancel_at_period_end` cancellation, and this codebase uses `CANCELLED` to mean *scheduled to* cancel (`payments.service.ts:84` and `upgradeSubscription` both treat it as live; the dashboard renders 「已排定取消」). `syncUserTier` selects `ACTIVE` only, so the user dropped to FREE the instant they clicked cancel, with up to a month paid: fortune windows collapsed, explain-element paid layers stripped, chat quota resnapshotted to 0. **RevenueCat already models this correctly** ("keep status ACTIVE until EXPIRATION"); Stripe was the odd one out | Removed the recompute. `customer.subscription.deleted` → EXPIRED → `syncUserTier` is the event that genuinely ends entitlement. **The old test asserted the bug** (`expect(user.update).toHaveBeenCalledWith({subscriptionTier:'FREE'})`) — replaced with its inverse plus a test that the CANCELLED write itself stays, since the UI and `reactivateSubscription` both depend on it |
| **M-H2** | `cancel` and `reactivate` reach `syncUserTier → refundStrandedPaidOnTierChange`, which **increments credits**, and were left on the global 100/min/IP. They are *sharper* than `/upgrade` — which did get the throttle, with a comment naming this exact hazard — because flipping status guarantees the tier change, whereas an upgrade between two paid tiers often changes nothing. Concrete loop: cancel→reactivate recovers a credit per partially-used chat extension, indefinitely | `@Throttle(5/min)` on both, matching `/upgrade` |
| **M-L1** | The `@Throttle` was pinned by nothing — deleting it was green, because the controller spec invokes handler methods directly and never reads route metadata | `payments-throttle.spec.ts` reflects the metadata for all three routes, with a positive control so a wrong metadata key can't make it pass vacuously |
| **M-M1** | **The ledger invariant held for no account in the system.** Three signup sites grant 3 credits via `user.create({ data: { credits } })` — a shape the A6 sweep's grep (`credits: { increment \| decrement }` and absolute `credits:`) did not match. This is the second undercount in that table, and the one that touches *every user* | `recordSignupBonusLedger` shared helper at all three sites. Deliberately non-transactional: a bookkeeping row must never be able to fail a signup. The upsert site ledgers only on the CREATE branch, or every profile edit would mint a phantom grant |
| **A-H1** | **The Sentry scrubber parity test faced one way.** It derived its key list from **web's** exports and probed the API side indirectly, so a key deleted from web vanished from the test's own iteration set. Reproduced: deleting `birthcity` and `fourpillars` from web left the suite green with all four pillars in the event verbatim — and web is the SDK running on the pages where birth data is typed | Exported the API lists; the check is now set equality in both directions. Mutation re-run: 3 failures |
| **A-H2** | **The Clerk `user.deleted` → `erasePersonalData` wiring had zero tests.** Deleting the call restored the C1 defect with a green suite. It is the only erase path for a Clerk account-portal or Dashboard deletion, and it runs on every in-app deletion too | `clerk-webhook-lifecycle.spec.ts` (11 tests). Mutation: 4 failures |
| **A-M1** | The two deletion doors had **opposite failure semantics**. `deleteAccount` documents that erase is not best-effort; the webhook wrapped the whole body in a catch that logged `User not found for deletion` for *any* failure, then returned 200 so Clerk never retried. A 30s transaction timeout mid-erase left everything in place behind a log line naming a cause that was not the cause | Only "no local user" is caught (explicit early return, 200). Everything else propagates → 500 → Clerk retries. Redis cache invalidation is the one step still allowed to fail quietly |
| **A-M2** | `apps/web/middleware.ts` re-implemented the azp parser inline, untested, without dedupe or the boot warning. Two green mutations: dropping the normalisation (after which `https://App.Example.com/` 401s every web page load while the API accepts the same token), and reverting the options object | Extracted to `apps/web/app/lib/authorized-parties.ts` with the warning; `authorized-parties-parity.spec.ts` asserts behaviour over a shared case table. Mutations: 7 and 3 failures |
| **A-L2** | `mask_all_element_attributes` was credited with stripping the query string. It masks `attr__*`; `$current_url` and `$referrer` are top-level properties, so every autocaptured event re-attached `?profileId=…` | Added `sanitize_properties` to strip query and hash from both |
| **M-L2** | The B5 launch checklist lists two token-minting surfaces; O3's reroute created a **third** (server-side `auth().getToken()` in the explain-element route) | Added to the decode list |
| **M-L3** | "Billing precedes generation at all 18 Anthropic call sites" is stronger than the code | Corrected in place with the actual ordering |

### Accepted, with triggers

- **M-M2 / M-M3 (RC F8 + F10)** — the proration hazard closed on Stripe is **wide open on RevenueCat** (`PRODUCT_CHANGE`/`UNCANCELLATION` mint a full unprorated month; the idempotency key is written before the grant reads it), and `handleConsumablePurchase` commits its idempotency row and grants in a *separate* statement, so a crash between them locks the customer out permanently. Both were already filed as F8/F10. **Re-rated: hard prerequisites for the IAP ship (AND5–AND7), not "payments slice" — they are the two live money defects on the provider that work is about to switch on.** The reading risk is the real hazard: after Phase 1 the Stripe code carries a 20-line comment asserting the invariant is enforced, and a reviewer who checks it there will conclude it is closed everywhere.
- **A-M3 — account deletion misses the COMPATIBILITY reading cache.** Comparisons cache under `generateComparisonHash` (different function, folds in the year); the purge enumerates `baziReading` and hashes with `generateBirthDataHash`. Every 合盤 cache row survives deletion permanently, and each holds the full interpretation for **both** parties — the second of whom never consented directly. ZWDS is the same shape (9 hash args written, 6 passed), bounded to legacy rows. `legal.controller.ts` carves out 「部分交易記錄」 for the retained financial tables, which is honest; it does not cover a cached compatibility interpretation, so the published 「永久刪除」 is **inaccurate for compat**. Nobody is harmed today (zero real users). **HARD PRE-LAUNCH GATE: fix the purge or amend the policy — the gate is the point at which that claim becomes true or false about real people.**
- **A-M4** — snapshots orphaned *before* the `deleteBirthProfile` pre-delete fix hold narrative text plus a `chartHash` and can never be reached again (`DailyFortuneSnapshot` has no `userId`). The source is closed; the stock needs a one-off cleanup query, not a code change.
- **A-L1** — F5 gave `getChatContextForFortune` a required `userId` so a forgotten gate is a compile error; its two siblings still take a bare id. Not exploitable — all four call sites validate session ownership at entry — but the shape lesson was not carried next door.

### The recurring defect, instances 7 and 8

A well-covered **helper** behind **untested wiring**. B3-a's `flush_counter` lifespan call site was #7; the Clerk `user.deleted` erase call is #8 — and #8 is the same *file* as #6, where the original finding was that the webhook did not erase at all. The fix landed; the test for the fix did not. `free-spend-vectors.spec.ts` even passes a `USERS_STUB` commented *"Irrelevant to signup-bonus vectors; stubbed for arity"* — which is exactly how a second call site stays invisible to a spec that constructs the controller.

**The instruction that would have caught both: when a fix adds a CALL SITE, the mutation to try is deleting the call, not breaking the helper.**

---

## Notes for future phases

- **Lint ratchet**: `apps/api/eslint-suppressions.json` records per-file violation *counts*. Adding one new `any` to a file at its budget un-suppresses **every** violation in that file, so lint failures can appear on lines you never touched. Fix = don't add the violation (`as unknown as T`), not raise the budget. Cost me a detour; documented so it doesn't cost the next person one.
- **Verification hygiene**: `cmd | tail` returns *tail's* exit code. Capture the real exit before piping, or a failing check reads as passing. This produced one wrong "baseline is clean" conclusion mid-audit before it was caught.
- **A suite that fails to *compile* reports zero tests, and the headline number just gets smaller.** Adding a constructor arg to `AdminService` broke two admin specs at the TS level; `tsc -p tsconfig.json` didn't catch it (tests aren't in that project) and jest showed "1375 passed" with no failing *test*. Always read `Test Suites:` alongside `Tests:`, and reconcile the total against the expected baseline + new tests — here 1416 + 25 = 1441, which is what it should have been.
- **Mocks must match the shape the ORM returns, including snapshot semantics.** A fake that hands back the live row object lets a later write change what an earlier read saw — the opposite of a real query — which can make a concurrency test pass against code with the guard deleted. See the A6 note. The same class of error: writing `undefined` as if it were a value, when Prisma reads it as "don't update this column".
- **Establish reachability from the state machine, not from field names.** The F11 regression came from defending against a state (`isDegraded && refundedAt`) that the generator cannot produce, and the test written to defend it hand-built exactly that impossible row. Before adding a guard, ask which code path writes the state it guards against.
- **Where you inject a simulated race decides whether the test discriminates.** Injecting a concurrent spend at a read the code under test doesn't reuse leaves both the buggy and the fixed implementation landing on the same number. Hook the statement whose atomicity is the actual claim.
- **A mock cannot validate SQL.** Any correctness that lives in a raw query (`GREATEST`, `LEAST`, `ON CONFLICT`, CTEs) is untestable with a fake `$queryRaw` — the fake just replays whatever answer it was told to. Either write an integration test or say plainly in the test name what is and isn't covered.
