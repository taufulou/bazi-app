# 八字時辰未知 (Unknown Birth Hour) — Session Handoff

**Read this first to pick back up after compaction.**

## Status: Phase 1 + Phase 2 SHIPPED (branch `feat/unknown-birth-hour`, NOT pushed, NOT in main)

Feature: a first-class **「時辰未知」** path that produces an honest **3-pillar (年/月/日)** Bazi reading when the user doesn't know their birth hour. Phase 1 = LIFETIME. **Phase 2 = LOVE + CAREER + ANNUAL + FORTUNE(日/月/年運) + CHAT, all crash-safe + AI-suppressed (committed 2026-06-13/14).** **Post-Phase-2 hardening "(a)" = N1/N3/N4 DTO/cache hardening + 2e UI polish, committed + line-audited + browser-verified 2026-06-14 (see «(a)» section below).** Phase 3 (COMPATIBILITY) not started.

## ⭐ Phase 2 — SHIPPED (5 commits, 2026-06-13/14) — read `.claude/plans/unknown-birth-hour-phase2-audit.md` for the detailed per-slice work-list + line-audit outcomes

Loop per slice: implement → independent sub-agent line audit → browser/endpoint spot-check → commit. Commits (newest first):
- **`a23c3cc` 2d chat guard** — chat 422'd at the engine DTO boundary (`ChatContextInput`/`FortuneChatContextInput` required `birth_time`); fixed → Optional + `hour_known`; threaded into `build_chat_context`/`build_chat_context_fortune` (+ MONTH/YEAR computes); `build_chat_context` surfaces top-level `hourKnown`; NestJS fetch helpers send `hour_known`; `chat-prompt-builder.buildPrompt` prepends the 時辰未知 directive gated `chatContext.hourKnown===false`. COMPATIBILITY untouched (its profiles stay hour-known).
- **`7e84351` FORTUNE** — `fortune-snapshot.helpers.ts` 3 fetch payloads send `hour_known: profile.hourKnown ?? true` (load-bearing — was silently fabricating the hour); 3 injectors in `fortune-prompt-builder.ts` gate `buildFortuneHourUnknownBlock` on `chart.hourKnown===false`; preflight/fetch types widened.
- **`c261382` 2c reading-page** — shared `ai.service.ts::buildHourUnknownSuppressionBlock` helper used by `interpolateLoveV2Fields`/`interpolateCareerV2Fields`/`interpolateAnnualV2Fields` (gated `data['hourKnown']===false`); frontend 五行/十神比重 charts (`ElementCapabilityChart`/`TenGodCapabilityChart`) take `hourUnknown` prop → 「※ 時辰未知…僅供參考」 caveat (wired from `AIReadingDisplay`).
- **`651f71a` 2b WRONG-OUTPUT** — ANNUAL `compute_pillar_impact_analysis` skips the blanked hour (no phantom 子女宮 row → no false 「子女宮平穩」 + no AI leak); LOVE `compute_spouse_star_analysis` lateMarriageIndicator→None, `classify_peach_blossoms` skips blank hour, `compute_marriage_timing_indicators` softens visible-spouse 不透出.
- **`268bf49` 2a engine safety** — the ONE real crash fixed: `ten_gods.calculate_weighted_ten_gods` missing the skip-empty guard → `STEM_ELEMENT[''] KeyError` (surfaced via CAREER); `hour_known` threaded through `calculate_bazi_with_all_pipelines` + the 3 FORTUNE DTOs/endpoints/wrappers (`monthly_enhanced`/`yearly_enhanced`); `chartContext` emits `hourKnown` (daily/monthly/yearly).

**Shared patterns (reuse for Phase 3 / future):** the deterministic suppression block is gated on `hourKnown === false` (strict) so **hour-known prompts are byte-identical → no cache version bump, no mass eviction**. `hourKnown` flows: engine top-level `chart['hourKnown']` (from `calculator.py:429`) → slim/chartContext → NestJS `data['hourKnown']`/`chatContext.hourKnown`/`FortuneChartContext.hourKnown` → injector gate. NestJS profiles load full-row (no `select:`) so `profile.hourKnown` is always present + survives `{...profile}` spreads.

**Bonus env fix (Phase 2 session):** `/reading/fortune` was 500ing for everyone — `html2canvas` declared in `apps/web/package.json` but **not installed**. `npm install --ignore-scripts` (main repo, not a worktree) restored it; page now 200. No tracked files changed. (Was briefly spawned as a task, then dismissed.)

## ✅ (a) Post-Phase-2 hardening — SHIPPED 2026-06-14 (1 commit, line-audited + browser-verified)

The N1/N3/N4 + 2e items that were deferred from the FORTUNE slice. NO migration, NO cache version bump, NO env var (engine restart only). Skipped a staff-engineer plan review (design already covered by the master plan's 3 review rounds; risk is mechanical) → straight to implement → per-slice line audit (2 parallel reviewers) → browser test.

- **N1 — `birth_time` required when `hour_known=True`** (`main.py`): shared `_HourKnownValidatedInput(BaseModel)` mixin with a `@model_validator(mode="after")` that raises when `hour_known` truthy AND `birth_time` falsy. 6 DTOs switched to it (`BirthDataInput` + `ChatContextInput` + `FortuneChatContextInput` + `DailyFortuneInput` + `MonthlyFortuneInput` + `YearlyFortuneInput`); `CompatibilityInput`/`CompatChatContextInput` inherit it via nested `BirthDataInput`. A malformed known-hour-no-time payload now **422s instead of silently producing a wrong chart** (the NOON placeholder would otherwise yield a different person's 時柱). Internal Python callers never construct these DTOs, so it's API-boundary-only. **Live-probe verified**: known-hour-no-time→422, unknown→200, known+time→200.
- **N3 — `birth_time: Optional[str]`** on `calculate_bazi` + `calculate_four_pillars` (typing only; `calculate_bazi_with_all_pipelines` was already widened in 2a).
- **N4 — L1b daily cache key** `(chart_hash, iso_date)` → **`(chart_hash, iso_date, hour_known)`** in `monthly_enhanced.py` (+ type annotation + docstring). An hour-known vs hour-unknown chart for the same person has different effective_gods/strength → must not collide. (`_flow_year_cache` was already 3-tuple from 2a.)
- **2e UI — annual_family 子女宮 note** (`AIReadingDisplay.tsx`): hoisted `hourUnknown` once (replacing 2 inlined copies), threaded into `AnnualSectionBadge`; the 家庭 strip now shows 「子女宮需出生時辰，此處僅評印星（長輩庇蔭）」 instead of a silent empty detail (the engine already blanks the hour palace per 2b; this makes the gap explicit — D8 "one voice").
- **2e UI — Fortune 時辰未知 banner** (`FortuneShell.tsx` + `.module.css`): a plain 3-pillar caveat line under the header on the fortune surface.

**🔑 DECISION — FortuneShell page banner, NOT `FortuneUpgradeModal`** (deviation from the §6 plan note): the plan said port the warning into `FortuneUpgradeModal`, but that modal is the **date-range subscription upsell** — a free/subscriber user viewing today's fortune never sees it, so the caveat would be invisible on the common path. Put it on the **fortune page itself (`FortuneShell`, wraps all 3 tabs)** instead. **Gating evolved during the browser test**: first written as `birthDate && !birthTime`, which only worked on the **day** tab (FortuneShell's `birthDate`/`birthTime` bind to the day-only `dataState`; month/year have their own state machines). Reworked to an explicit **`hourUnknown` prop** derived from the active profile's authoritative `hourKnown` flag (`activeProfileForDisplay?.hourKnown === false`, resolved from the tab-independent `profiles` list) → now correct on **day/month/year alike**. `FortuneUpgradeModal` left untouched.

**🔑 太歲 guard (line-audit Item 5 fix)** — `annual_enhanced.py::compute_tai_sui_analysis` looped all 4 pillar branches incl. the blanked hour. It was "safe by accident" (the empty branch fails all 5 太歲 lookups), but inconsistent with the Phase 2b skip-empty pattern. Added explicit `if not natal_branch: continue` + excluded empty branches from the 三刑 pool, with regression test `test_annual_no_phantom_hour_taisui`. Browser-confirmed the 太歲 badge shows no phantom 時柱太歲 (only 日柱破太歲).

**(a) verification:** engine **2965 pass** (9 new: N1×6, N4×1, 太歲×1, +fixture; only the documented pre-existing `test_roger_laopo_full_preanalysis` fails); web `tsc` 124-error baseline unchanged (0 new); line audit backend clean + frontend 4 findings (3 non-issues — globals resets `<p>` margin, `profileBirthTime` is `''` not `"null"`, `annual_relationships` already guarded — + the 太歲 fix). Browser E2E: fortune banner on **day/month/year** ✓, **known-hour negative control** shows no banner ✓, annual_family 子女宮 note renders ✓, no phantom 太歲 ✓.

## ⚠️ Testing reality (honest — comprehensive run still PENDING)
Per-slice verification only: independent line audits (5) + targeted regression unit tests (engine `test_unknown_hour.py` **22 tests**; NestJS gate specs `fortune-prompt-builder.hour-unknown.spec.ts` + `chat-prompt-builder.hour-unknown.spec.ts`, 6 tests) + browser spot-checks on **~1 anchor chart** (1995-11-08; Roger/Laopo in unit tests). Live-AI-verified: CAREER + ANNUAL readings, FORTUNE daily+monthly (via endpoint). **NOT yet done** (= the comprehensive run, always planned for after Phase 3): a written test matrix; chart diversity (DM strengths/從格/boundaries/genders × each type); **LOVE live AI** (unit+audit only), **FORTUNE yearly live**, **chat live message** (env-blocked); the **A2-style 3-parallel Bazi-master narrative-QA gate**; **web RTL tests** for the new React UI; systematic hour-known byte-identical diff; COMPATIBILITY. Crash-safety + no-fabrication on common paths = well-covered; comprehensive matrix + AI-quality grading + frontend tests = pending.

## Key documents (read in this order)
1. **Approved implementation plan + full review/impl log:** `/Users/roger/.claude/plans/i-just-switch-to-quizzical-church.md` — the canonical plan (12 decisions D1–D12, Central engine strategy, §1–8, Phasing, Verification, Round 1–3 staff-engineer review log, Phase 1 IMPLEMENTED + browser-test log). **Source of truth.**
2. Comprehensive design doc (repo): `.claude/plans/plan-unknown-birth-hour.md`
3. Research memo (gold standard / competitors / engine impact): `.claude/plans/research-unknown-birth-hour.md`
4. Staff-engineer review files (Round 1–3): `.claude/plans/i-just-switch-to-quizzical-church-agent-{a5dca…, a2ba…, abf9…}.md`

## Locked product decisions (D1–D12) — see plan for full table
D1 first-class 時辰未知 → honest 3-pillar, never silent-guess for analysis. D2 no in-place unlock CTA (only a NEW reading with the hour). D3 immutable hour state (set at creation). D4 keep exact time picker + toggle. D5 no 定盤/tiers. D6 acknowledgement at the spend-credits step (NOT at 排盤) — revised 2026-06-13. D7 用神 "A + auto-detect tail". D8 in-place 「需要出生時辰」 notes + global hint. D9 compat partial (Phase 3). D10 fortune available (Phase 2). D11 ZWDS ignored/dropped. D12 header basis line + 三柱 badge.

## What Phase 1 delivered (architecture)

**Central engine strategy** (the load-bearing bit): when `hour_known=false`, use a **transient NOON placeholder** for y/m/d + 大運 datetime internals (noon avoids the 23:00 子時 day boundary), then **blank the hour pillar** (empty stem/branch) before `calculate_four_pillars` returns. The empty hour stem is the canonical "unknown" signal (`is_hour_unknown`). Every analytical loop has a **skip-when-empty guard** (`if not pillar['stem']: continue`) so 五行/strength/十神/從格 compute on 3 pillars without KeyError. 命宮/身宮 → null (need 時支); 胎元 (month) / 胎息 (day) survive.

**Hour-dependence (engine-verified):** SURVIVE = 日主, 年/月/日 三柱, 月令格局, 配偶宮=日支 + 紅鸞/沖日支, 胎元/胎息, 大運 sequence, 生肖. LOST = 時柱 + 時柱十神/神煞, 子女宮, 晚年, 命宮/身宮, 時支神煞. DEGRADED = 用神/五行比重 (3-pillar, flagged), 起運 age (≤±2mo via noon), 神煞 completeness.

**D7 用神 flags** (on `dayMaster`): `hourUnknown:true`, `yongShenConfidence:'reduced'`, `yongShenCaveat:'borderline'` when V2 score within ±3 of a band boundary (boundaries `(25,40,55,70)`), `geJuStatus:'undetermined_without_hour'` when 從格 detected.

**API:** FastAPI `BirthDataInput.birth_time` Optional + `hour_known` field; Prisma `birthTime String?` + `hourKnown Boolean @default(true)` migration; NestJS DTO `@ValidateIf((o)=>o.hourKnown!==false)`; `hour_known` threaded NestJS→FastAPI→engine; cache hash sentinel `?? 'HOUR_UNKNOWN'`; **deterministic AI suppression block** in `interpolateLifetimeV2Fields` (gated `data['hourKnown']===false` → cache-safe).

**AI suppression block** (`ai.service.ts::interpolateLifetimeV2Fields`) enforces: no fabrication/detail of hour items; **in-place 「需要出生時辰」 note, NOT silent omission** (D8); **神煞 false-negative guard** (禁止「命中無某神煞」); 用神「（時辰未知，僅供參考）」; 格局待確認 when undetermined; **D2-aligned 補時辰 phrasing** (「日後得知時辰，可另建新的命盤查看完整分析」, never 「我可以為你提供」).

**Frontend:** `BirthDataForm.tsx` 時辰未知 toggle **below the time row** (disables time dropdowns + plain hint); **no submit-time modal** — 開始排盤 → free 3-pillar preview directly, and the acknowledgement rides inside the shared `UnlockConfirmModal` via a `hourUnknown` prop (gated `!!chartData && !chartData.fourPillars?.hour?.stem`) at 解鎖完整報告; `BaziChart.tsx` 時柱 column placeholder + header tag + basis line (`text-align:left`) + 公曆「（時辰未知）」; nullable `birthTime` types across `birth-profiles-api`/`readings-api`/`date-time-utils`. Wording is plain/beginner-friendly throughout (revised 2026-06-13 — see refinements below).

## Phase 1 UI/UX refinements (2026-06-13) — browser-verified. **These set the style Phase 2/3 must follow.**
Owner-feedback polish on the shipped LIFETIME flow. Full rules in the master plan (D6, D8, §5, §6, §7 + the "Phase 1 UI/UX refinements" review-log entry). The four changes:
1. **Toggle below the time picker** — `我不知道出生時辰` sits under the 時／分／午別 row (was above) + plain hint on tick. `BirthDataForm.tsx` (+`.module.css`).
2. **Acknowledgement at unlock, not at 排盤** (D6) — deleted the submit-time modal (JSX + state + dead `.modal*` CSS); `handleSubmit`→`performSubmit()`. Warning now rides inside `UnlockConfirmModal` via `hourUnknown` prop, gated `hourUnknown={!!chartData && !chartData.fourPillars?.hour?.stem}`. **Reuse this prop on every paywall surface** — LIFETIME/LOVE/CAREER/ANNUAL share `UnlockConfirmModal` (free); **FORTUNE (`FortuneUpgradeModal`) + COMPATIBILITY must port the block in Phase 2/3.**
3. **Plain wording everywhere** (D8) — basis line / form hint / unlock-modal warning / AI in-place notes all lead with 「由於未提供出生時辰…」, gloss 時柱→「出生時辰那一柱」. Keep the unlock-modal list ↔ AI in-place notes in sync.
4. **Basis line left-aligned** — `.hourUnknownBasis { text-align:left; }` (sits in a centered header). `BaziChart.tsx` (+`.module.css`).

Files touched: `BirthDataForm.tsx`/`.module.css`, `UnlockConfirmModal.tsx`/`.module.css`, `BaziChart.tsx`/`.module.css`, `reading/[type]/page.tsx`. Frontend-only (no engine/NestJS rebuild). Browser E2E (fresh chart 1995-11-08 時辰未知): toggle-below + plain hint ✓; 開始排盤 → free preview, no popup ✓; basis line `text-align:left` + plain copy ✓; 解鎖完整報告 → `UnlockConfirmModal` warm-amber warning block (5 plain bullets + 「僅供參考」 + 「另建新命盤」) above feature grid + 💎 cost ✓; cancelled (no credits); zero console errors.

## Verification (all green)
- Engine: **9 new `test_unknown_hour.py`** (Roger neutral + Laopo weak-DM no-crash) + **2944 suite pass** (only the documented pre-existing `test_roger_laopo_full_preanalysis` fails). API `tsc` clean; web `tsc` only the pre-existing ChatDrawer JSX-identity error.
- **3-parallel line audit** caught 2 criticals my single-anchor test missed (`check_cong_ge` crash on weak DM; frontend `callDirectEngine` never sent `hour_known` → 422) + 3 high/med — all fixed & re-verified.
- **Browser E2E (LIFETIME, signed-in real flow): PASS** — toggle→modal→engine→chart→**live Claude AI**, no 422, no crash, zero fabricated hour content, 角色卡 rendered. Re-verified the in-place 子女關係 「⚠️時辰未知限制說明」 note on a fresh chart. 6 test credits used.

## Calibration anchors
- Roger `1987-09-06 16:11 吉打 male` known = 丁卯/戊申/戊午/庚申, DM 戊; **unknown-hour** = 丁卯/戊申/戊午/(blank), 用神 火, 旺衰 中和 42分 (borderline), 食神格. y/m/d byte-identical to known.
- Laopo `1987-01-25 12:00 台北 female` = 丙寅/辛丑/甲戌/壬申, DM 甲 weak — the weak-DM `check_cong_ge` regression anchor.

## ⚠️ DEFERRED / remaining (after Phase 2)
- **Live chat-message browser test** (2d) — NOT run; env-blocked: CAREER chat is **disabled** in this dev env (`CHAT_ENABLED_READING_TYPES` unset), and LIFETIME chat needs an unlocked reading (3 credits). Mechanism is identical to the browser-verified reading-page/FORTUNE injectors + locked by gate unit tests. To run it: enable CAREER chat OR unlock a LIFETIME hour-unknown reading, open the drawer, ask a 子女/晚年/命宮 question, confirm the AI declines with 「需要出生時辰」 (no fabrication).
- ~~**N1/N3/N4 DTO-hardening**~~ + ~~**2e UI polish**~~ — ✅ DONE in the «(a)» commit (2026-06-14). 2e's fortune caveat went on `FortuneShell` (page banner, all 3 tabs), NOT `FortuneUpgradeModal` — see the «(a)» section above for the rationale + the 太歲 guard.
- **Comprehensive test run** (always planned for after Phase 3): test matrix × chart diversity (DM strengths/從格/子時·立春·leap boundaries/genders) × all reading types live; A2-style 3-parallel Bazi-master narrative-QA; **web RTL tests** for the new React UI; systematic hour-known byte-identical diff.
- Minor: ElementExplanation 時柱-click note; UpdateBirthProfileDto birthTime immutability (benign — engine keys off hourKnown); modal a11y; ProfileCard 時辰未知 marker.

## Phase 3 (next major) — COMPATIBILITY
COMPATIBILITY (合盤) partial + 神煞 partial-scan. Per the audit (2d mapping), `build_chat_context_compat` + `compatibility_*` pipelines call `calculate_bazi_with_all_pipelines` WITHOUT `hour_known` → would fabricate hours for an hour-unknown partner. Compat profiles currently stay hour-known (Phase 1 decision: `toBirthDataFormValues` keeps `hourKnown:true` on the compat path), so it's not live-broken, but enabling hour-unknown compat needs: guard `compatibility_enhanced.py`/`compatibility.py` 4-pillar scoring loops (the `i==3` skip exists only in `compatibility_romance_preanalysis.py`); thread `hour_known` through the compat DTO/endpoint/`build_chat_context_compat`; D9 partial-合盤 (day-branch 配偶宮 core survives, flag hour-dependent cross-interactions); compat AI injector suppression block.

## Gotchas carried forward (IMPORTANT)
- **Servers must be restarted DETACHED** (`nohup … & disown`), NOT via `run_in_background` — the latter gets killed between turns. Engine: `cd packages/bazi-engine && source .venv/bin/activate && nohup uvicorn app.main:app --host 0.0.0.0 --port 5001 > /tmp/bazi-engine.log 2>&1 & disown`. NestJS (after `nest build`): `cd apps/api && export ANTHROPIC_API_KEY="$(grep '^ANTHROPIC_API_KEY' .env | cut -d= -f2)" && nohup node --import tsx dist/main.js > /tmp/nestjs.log 2>&1 & disown`. Next: `cd apps/web && nohup npx next dev --port 3000 > /tmp/nextjs.log 2>&1 & disown`.
- **After editing `ai.service.ts`/services → REBUILD NestJS** (`../../node_modules/.bin/nest build`) + restart; the running PID has stale code.
- **Next.js HMR stale across session boundary** → hard-reload (Cmd+Shift+R) or restart the dev server; the toggle/changes won't show otherwise.
- **Browser:** use `http://127.0.0.1:3000` (HSTS dodge); Clerk handshake makes you signed-in (Roger). Fill React-controlled inputs via the native-setter + dispatch input/change pattern.
- **Reading cache has NO prompt version** in its key (`birthDataHash` = birthDate|birthTime-sentinel|city|gender|readingType|targetYear). A prompt change won't reflect for the SAME chart unless you flush Redis or use a DIFFERENT birth date.
- **Migration applied non-destructively** on dev DB (direct `ALTER` + `prisma migrate resolve --applied`) because `prisma migrate dev` wanted to RESET (pre-existing folk-content migration checksum drift — chronic, unrelated). For prod: `prisma migrate deploy` applies `20260609000000_add_hour_known_to_birth_profiles` cleanly.
- **Multi-server restart `.env` trap (NEW):** restarting engine + NestJS in one Bash command — the engine's `cd packages/bazi-engine` PERSISTS into the NestJS line, so `grep '^ANTHROPIC_API_KEY' .env` runs from the wrong dir → empty key + `node dist/main.js` from wrong dir (no dist). Restart NestJS in its OWN command: `cd /Users/roger/Documents/Python/Bazi_Plotting/apps/api && export ANTHROPIC_API_KEY=... && nohup node --import tsx dist/main.js …`.
- **Browser form-fill (NEW, reliable pattern):** `form_input` does NOT fire React `onChange` for controlled inputs/checkboxes (controlled state reverts). The reliable method is the **native-setter + dispatch**: `Object.getOwnPropertyDescriptor(HTMLSelectElement.prototype,'value').set.call(sel,v); sel.dispatchEvent(new Event('change',{bubbles:true}))` for selects; `...HTMLInputElement...'value'...` + `new Event('input')` for text; `...'checked'...` + `new Event('click')` for checkboxes. Also: the browser window got resized to **924px** mid-session → `getBoundingClientRect` (CSS px) vs computer-tool screenshot px mismatch → coordinate clicks miss. Use **JS `.click()`** on the element (e.g. `[...buttons].find(b=>b.textContent.trim()==='開始排盤').click()`) to avoid coordinate math.
- **CAREER chat is DISABLED in this dev env** (`CHAT_ENABLED_READING_TYPES` unset → CAREER off). LIFETIME chat is always on. To live-test chat, set `CHAT_ENABLED_READING_TYPES=LIFETIME,LOVE,CAREER,ANNUAL,COMPATIBILITY,FORTUNE` in `apps/api/.env` + restart Nest.
- **`html2canvas` was declared but not installed** (broke `/reading/fortune` SSR with `Cannot find module 'react-dom/server.browser'` / `Can't resolve 'html2canvas'`). Fixed via `npm install --ignore-scripts` (main repo). If node_modules is reset, re-run it.

## Deploy notes (when this reaches main)
- `prisma migrate deploy` (applies `hour_known` + nullable `birth_time` — Phase 1 migration). **No new migrations in Phase 2.** No new env vars (but ensure `CHAT_ENABLED_READING_TYPES` includes the types you want chat on).
- **No cache version bumps in Phase 2** — all suppression is gated `hourKnown===false` so hour-known prompts/contexts are byte-identical; hour-unknown is a distinct chart-hash bucket. No mass eviction. Flush Redis only if you want existing hour-unknown readings/chats regenerated.
- `npm install` (or `--ignore-scripts`) is needed wherever node_modules lacks `html2canvas` (declared in `apps/web/package.json`).
- ZWDS being dropped — left compile-only widened with TODO markers.
